# ScanGrade - AI Agent Context

## Goal
- Complete notification system with WhatsApp-like percakapan (threaded 1-on-1 chat), pengumuman (broadcast), and UU PDP data retention compliance, with strict RBAC separating student↔teacher messaging (no inter-student conversations or broadcasts).

## Constraints & Preferences
- Supabase project: `roshkbkbzgfedowozfo` (region: ap-southeast-1)
- Python 3.12.10, Flask 3.1.1, supabase-py 2.12.0, Alpine.js v3.14.8, Tailwind CSS (compiled locally, see below), Chart.js 4.4.7
- Two Supabase clients: `get_supabase()` (service key) and `get_auth_client()` (anon key)
- Must support 500 concurrent students with spotty WiFi (3 floors, uneven coverage)
- Offline-first: localStorage first, sync to server when online
- `MAX_CONTENT_LENGTH = 50MB`
- Canvas data saved as PNG (not JPEG — was causing black overlay bug)
- **Tailwind CSS**: Local compiled CSS (`npm run css:build` after template changes). NOT CDN — users have spotty WiFi.
- **A Tailwind utility name must never be built at render time.** Tailwind reads the template as text, so `from-{{ color }}-600` matches nothing, generates nothing, and the element silently keeps its ancestor's gradient — that is how two of the three "Log in" buttons on `/demo` shipped as white text on a white box. Name each utility literally; `demo.html` uses `{% set EMAIL_COLOR/COPY_BUTTON/LOGIN_COLOR %}` maps. Guarded three ways in `tests/unit/test_tailwind_class_names.py`: no interpolation flush against an identifier inside a class attribute; no utility name **assembled in JavaScript** — `:class="'from-' + color"`, `` :class="`bg-${tone}-500`" `` and `.concat('bg-', tone)` leave the name half-literal, which rule 1 cannot see (the glue is JS) and rule 2 cannot see (the name never appears whole); and every colour utility a class-bearing attribute **or a script** names must exist in the committed `tailwind.css` (the `<script>` and `x-data` regions are where `ai_settings.html`'s provider palette and `schools.html`'s toggle live). All three run in `deploy/theme_gate.sh`, so they gate the pre-commit hook and the VPS auto-deploy, not just the suite
- **Legibility floor + mobile layout**: `app/static/css/theme.css` (linked from `base.html`) maps `text-[6..11px]` up to ≥12px in one block, so 546 call sites did not have to be edited by hand (the only documented below-floor exemption is the OMR answer-sheet mockup in `tools/generate_answer_sheet.html`, where 4-5px *is* the artefact). Every non-print table with ≥4 columns sits in an `overflow-x-auto` container **and** carries a `min-w-[...]` — without the min-width the table squeezes instead of scrolling. Four print documents (`monitor.html`, `print/report_card.html`, `student/result_detail_pdf.html`, `teacher/print_exam_report.html`) are exempt from the table rule because a phone is not their target. Guarded by `tests/unit/test_mobile_layout.py`
- **The cost of a page is its round-trips, so a row is asked for once.** Supabase answers in ~100-165 ms from this box, and the *cold* student dashboard issued **12** calls: `profiles` four times (three of them for a `class_id` the session already carried), `submissions` three times over the same rows (the row carries `status`, so the "answered" and "retracted" sets come from one read), `teacher_assignments` counted twice with an identical filter where the second answer overwrote the first, and `schools` twice for columns no template renders (`base.html` reads `g.get('school_info')`, which nothing ever sets — the dashboards' `school_info` was dead data, and the per-school favicon has never activated). It is now **6**, and the exam list went 4 → 2. `app/utils/req_cache.py` holds the two lifetimes that make that possible: `memo()` for this request only, in `g` so it cannot leak into the next (the dict it replaced was keyed by `id(request)` and never evicted — it grew for the life of the worker and could serve another request's row), and `ttl()` for rows identical for everyone in a school (name, logo, feature flag, classes, subjects, subject count, a class's active whiteboards), which is what turns 500 students into one query per TTL instead of five hundred. `class_id` rides on the cached session, so no student page reads `profiles` for it. Every writer invalidates what it changed, and `tests/unit/test_round_trip_budget.py` fails if an `invalidate_*` helper loses its call site, if a duplicate query reappears in the two hot routes, or if `id(request)` returns as a cache key
- **A stylesheet the browser can cache beats a smaller one it must re-download.** `base.html` carried the theme tokens, the dark-mode remap, the light-mode corrections and the legibility floor as one **36 KB `<style>` block**, so every page carried all of it inside its HTML: an inline block cannot be cached, nginx re-gzips it on each request, and a phone re-downloads it on every navigation. It is `app/static/css/theme.css` now, linked from `base.html`, and the same probe on the same box measures every student page **36 KB lighter** — dashboard 79 → **42 KB**, exams 75 → **39**, results 78 → **42**, settings 93 → **57**, comms 117 → **81** — with render CPU down only where it is measurable (dashboard 8.2 → **5.0 ms** warm; cold is Supabase, not rendering). It does **not** raise the concurrency ceiling by itself, and the measurement says why: during an 80-session run of `loadtest_concurrent.py` the box sat at **us+sy 85% median / 97% p90, `wa` 0** — CPU-bound, so capacity follows CPU per request and worker count, not bytes (same command: 2450 → 2578 requests, p50 513 → **374 ms**, 0 errors, p95 inside run-to-run noise). `asset_v()` versions the URL by content hash, which is what makes nginx's `immutable, max-age=31536000` safe — and `tailwind.css` now uses it too, because until now a committed CSS rebuild had no way to reach a browser that had already loaded the old one. `tests/unit/test_theme_stylesheet.py` fails if the block is inlined into `base.html` again, if the link moves before `tailwind.css` (which loses the dark remap in the cascade, silently), if a Jinja tag appears in the file, or if either stylesheet is linked by a bare path; the two guards that read that CSS were repointed at the file rather than relaxed, and all three run in `deploy/theme_gate.sh`
- **Inter font**: Local TTF files in `/static/vendor/inter/`, NOT Google Fonts CDN
- **Midtrans Snap.js**: vendored at `/static/vendor/midtrans/<build>/veritrans.co.id/snap.js` (build = `production` or `sandbox`), with the CDN only as a load-failure fallback. **The `veritrans.co.id` directory is load-bearing, not decoration**: Snap.js finds its own `<script>` tag by matching that string (or its API host, which carries a scheme and so can never match a same-origin path), then reads `data-client-key` off the tag it found. Rename the directory and the key silently becomes empty — the SDK loads fine, no error is raised, and the payment iframe comes up without a merchant. Guarded by `tests/unit/test_snap_vendored.py`
- The two Snap.js builds differ **only** in the host they hardcode, so they are not interchangeable: the sandbox build sends payments to sandbox (where they silently do not count), the production build takes real card details
- Default timezone UTC+7 (WIB), configurable per-user and per-school
- UI language: **English (default)** with an Indonesian toggle (`localStorage.sg_lang`) — a stored choice always wins. The default is decided in exactly one place, `base.html` (`default_lang|default('en', true)`), read both by `<html lang>` and by the Alpine `lang` scope; no route passes `default_lang` (a guard in `tests/unit/test_language_toggle.py` fails if one does). Strings are bound inline as `t('Indonesia','English')`; plain JS outside Alpine uses `window.sgT()`. The `TRANSLATED` list in `tests/unit/test_language_toggle.py` is the contract of which pages must stay bilingual — the five role dashboards (student/teacher/admin/super_admin/admin_sekolah) plus the working pages, the three role tutorials, `/demo` and the exam paper. Email addresses are excluded from that check: a demo credential like `guru_mtk_smp@scan-grade.app` is data, not copy
- **The i18n sweep reads card bodies, not just text nodes.** A card here is usually an Alpine binding — `<p x-text="t('Total','Total')">`, or a value out of `x-data` that a later `x-text` renders — and the original check read only `>text<`, so an entire untranslated card passed. The sweep now also reads the *string literals* inside text-rendering bindings (`x-text`, `x-html`, `title`, `placeholder`, `aria-label`, `alt`, `value`), inside `x-data`, and inside every `<script>` (alerts, status lines, `.textContent` writes). Three things make that honest rather than noisy: Jinja is **blanked before reading** (a text node containing `{{ }}` never matched the old pattern at all, and a `t()` whose argument holds a Jinja ternary had its pair split in half); an Indonesian literal counts as translated only when its partner is adjacent — the three shapes this app writes are `t('id','en')` / `sgT('id','en')`, `['id','en']`, and `{ id/en }` or `{ badgeId/badgeEn }` keys — and the partner must look English, so `['Simpan','Batal']` is still an offender; and a literal that is a path (`/api/…`) or a role slug (`guru`, `murid`, `admin_sekolah`) is data, the same string in both languages. `label` and `para` were removed from the marker set for the same reason: both are English words used as code here (`k.label`, `para_`, `p.append('label',…)`), and a guard that fails on code gets deleted. Adding card vocabulary (`aksi`, `tersedia`, `tren`, `penalti`, `mulai`, …) is how the five dashboards were found to be untranslated in the first place

- **Translation progress is a number now, and the deploy refuses to let it go backwards.** "30 pages are translated" said nothing about the other half, and the sweep only reads the pages someone already listed — so the backlog was invisible and invisible progress is how it goes backwards. `deploy/i18n_coverage.py` computes, per template, **pairs / (pairs + leftovers)**: the bilingual units it carries over the copy that still needs to, where a pair shape is `t('id','en')` / `sgT(…)` / a `lang === 'en' ? … : …` ternary / an `['id','en']` array with an English partner / an `{id/en}` or `{badgeId/badgeEn}` key. A pair is a numerator and **never** a denominator (counting a translated string as both is how a coverage number flatters itself), a template whose copy is all shared vocabulary is 100% and not 0%, and emails and role slugs are data. Measured today: **115 templates, 1593 bilingual units, 1376 leftovers — 53.7% overall, 38 templates with nothing left to translate, and 49 with no version in the other language** (`compliance/privacy.html` alone holds 216 strings). Two floors, because a percentage alone can be gamed: `pairs` never falls (exact — bilingual units are not deleted) and coverage never falls by more than `PCT_SLACK = 0.5` pp (one new untranslated string on a 200-unit page). Exit codes follow the other gates: 0 pass · 1 a page translates less · 2 could not measure — a template with **no recorded floor** is 2, not 0, so a new page's number cannot quietly start at zero, and a page that is *newly* frozen (pinned to Indonesian while carrying pairs, which makes those pairs dead — 32 templates are in that state today and it is reported, not failed) is 1. `--write-baseline` records the floors and is deliberately **never run by the deploy**: a gate that raises its own floor while a release is passing makes the regression the new yardstick, and writing inside the checkout mid-deploy would leave the tree dirty for the next `git pull`. The baseline is committed, LF-only (`write_text` translates `\n` to `\r\n` on Windows by default, so the first one came out CRLF and every later write on the other platform showed as a whole-file diff) and `--show <template>` prints the actual strings, so the number can be acted on. It runs in `deploy/theme_gate.sh` — which the pre-commit hook *and* the auto-deploy already run — and the gate prints the table, because the number is the point. Guard: `tests/unit/test_i18n_coverage.py` (25 tests) — the two readers **agree template-by-template** (the tool copies the sweep's vocabulary rather than importing the test suite, so an equality assertion is what keeps the copy honest), the metric is not gameable, the floors cover the tree and are never above it, the exit codes, and the wiring — mutation-checked, **10/10 injected defects caught**, one of which had to be *tightened* first: `'deploy/i18n_coverage.py' in src` was satisfied by the gate's own failure text, so deleting the run still passed. Three defects came out of the first run: the text-node pass read a **JS comment** in `base.html` as untranslated copy (an apostrophe in `// Alpine's \`t()\` …` opened a literal, and the comment ran to the next `<`), so script and style bodies are now removed before that pass in both readers; `_PAIR_KEY`'s `\w*` prefix applied to the lowercase `id`/`en` made `murid:'Murid'` — a role slug — read as a catalogue key, excusing a real leftover in `shared/comms.html`; and the gate's own failure text told the reader to run `python audit_page_lang.py --templates`, a file that **does not exist in the repository**, which is now a general rule (`test_every_tool_the_gate_names_exists`). It also found two partials nobody had listed — `teacher/_results_table.html` (table headers `Siswa`, `Penalti`, `Waktu`, `Aksi`) and `teacher/wb_toolbar.html` (tooltips `Hapus`, `Siswa`) — rendering hardcoded Indonesian inside *translated* pages; they are bound as pairs now, which took pairs 1596 → 1605 and leftovers 1386 → 1369. Partials stay off `TRANSLATED` on purpose: that list's own assertion requires every entry to extend base.html ("it has no toggle to honour"), so their copy is held by the coverage floor instead of by relaxing that rule
- **`t()` re-renders, `sgT()` does not**: an expression containing `t(...)` is re-evaluated when `lang` changes because `t()` reads `this.lang`; a string `sgT()` already assembled is frozen in the language it was built in. That is correct for a message shown once (an alert, a violation banner that just fired), and wrong for a label that stays on screen — `student/take_exam.html`'s sync label, its violation banner and its device bar are rebuilt by `relabelLocale()`, which `init()` runs from a `$watch('lang')` and which also re-invokes the device probes `initStatusBar()` stashes. No label may be *declared* as `name: sgT(...)`; `tests/unit/test_language_toggle.py` fails if one is, or if a stored label is unreachable from the rebuild hook. The exam page needs three language controls (exam bar, fullscreen overlay, agreement modal) because each overlay covers the bar and the navbar's button
- **A page declares the language of its own copy, and every part that switches declares itself.** `<html lang>` is what a screen reader uses to pick a voice, and this app has two halves that disagree about language *on purpose*: a page whose own copy was never translated pins itself with `{% set content_lang = 'id' %}` and must go on announcing Indonesian whatever the reader chose (a toggle cannot translate copy that does not exist), while the chrome — sidebar, top bar, both bottom navs, footer — follows the toggle on **every** page. That combination is what makes the marking necessary rather than decorative: on a pinned page the document is Indonesian and the navigation is English, so each region that switches carries `:lang="lang"` (region-level on purpose — in this chrome every string is a pair, so the region *is* the smallest element that contains the change), and so does any label inside a pinned page that renders a `t()` pair (measured: 23 sites across 15 templates, e.g. the exam list's `3 exams` ⇄ `3 ujian`, which now reports `lang="en"`/`lang="id"` matching its own text). Pinning is resolved in one place — `{% set _content_lang = content_lang|default('') %}` feeds both `<html lang>` and `data-content-lang`, and `setLang()`/`docLang()`/the early script all read that one attribute, so a stored choice never overrides a pin. Guard: `tests/unit/test_content_lang.py` (the `<html>` tag resolves both, the pin rides on the attribute every reader consults, the early script skips the stored choice when pinned, `setLang()` consults it, no other template computes `documentElement.lang`, every language control *calls* `setLang(` rather than merely mentioning it in a comment, the five chrome regions stay marked, nothing that follows the toggle is left unmarked, and no fixed `lang=` appears where copy switches — except a run bound to `x-show="lang===…"`, which is the alert macro rendering both halves). Mutation-checked, 9/9 injected defects caught. Verified live in both toggle positions: on a pinned page `<html lang>` stays `id` while the chrome and the pair-bearing labels flip, and on a translated page the document itself flips
- **A role signs out at its own door, and the door it is *for* is written once.** The app has two login pages and each says out loud who it belongs to — `/auth/login` is headed "Masuk Admin" and `/auth/login-user` "Masuk Guru / Murid" — so landing a teacher on the admin page is a dead end they escape only by spotting the small "Guru/Murid?" link. `logout()` already contained that intent (`redirect("/auth/login-user" if g.get("user_role") in ("guru", "murid") else "/auth/login")`) and it **never once fired**: `g.user_role` is filled by `login_required` and by nothing else, and logout deliberately sits outside that decorator because clearing the cookies has to work for a session that has already ended — so `g.get` answered `None` on every request and all four roles went to the admin door (verified against the local server: four roles, four times `/auth/login`). `_unauthorized()` had the same shape, hardcoded, so a teacher whose session timed out was told to log in again on a page headed "Masuk Admin". It is now one function, `login_door_for(role, path)` in `app/utils/auth.py`: the role decides, and when the role is unknown the **URL being opened** does, because the space is already partitioned by role (`/student`, `/teacher`, `/admin-sekolah`, `/super-admin`) and both doors can sign anyone in — a misread costs a click, not an authorization call. `session_role(token)` reads the session **cache first**, which is what keeps an expired token from losing its role at the exact moment the reader clicks logout. Three callers, one mapping: `logout()`, `_unauthorized()`, and the 401 handler. Guard: `tests/unit/test_login_door.py` (the mapping including the legacy `teacher`/`student`/`admin` names, each role's logout `Location`, **the destination page's own heading** so a role-*aware* redirect pointed at the wrong page still fails, that logout still clears both cookies, the idle/absolute-timeout path, the 401 handler, and a source rule that `logout` reads `session_role(` and not `g.get("user_role")`). Mutation-checked, 8/8 injected defects caught. Verified live: a student clicking Logout in the sidebar and a school admin doing the same land on "Teacher / Student Login" and "Admin Login" respectively, and an anonymous `/teacher/dashboard` now answers 302 to `/auth/login-user` while `/super-admin/dashboard` answers 302 to `/auth/login`
- **The entry door speaks both languages, including its error messages.** All twelve `/auth` pages (login, login-user, register, activate, forgot/verify/reset/set-new-password, and the four success pages) render the `content_noauth` branch, which carries **no navbar** — so they carry their own button from `app/templates/auth/_chrome.html`, the same convention as the landing page and `/capacity`. Page copy is bound with `t()`, but a *message* does not come from the template: the route builds it, and the language is in `localStorage` where the server cannot see it — so `app/utils/auth_messages.py` holds one `(id, en)` pair per message, the route passes the pair, and `alert_body()` renders **both** halves (with `x-cloak`, so English — the app default — is what paints first) for Alpine to choose between. Interpolating a message into an Alpine expression instead would break on a school name containing an apostrophe, which is what `test_a_text_binding_never_escapes_a_quote` exists for. A **plain string** (a Supabase error) still passes through untouched: that is data. `_unauthorized()` flashes a pair too, so the session notice on the login page follows the toggle, while the JSON path takes the Indonesian half — `jsonify({'error': pair})` would hand an API client an array where it expects a string, and `first()` is that boundary. The old `rate_limit_message()` returned a bare string and is no longer used by the door. Guard: `tests/unit/test_auth_messages.py` (every pair non-empty, both halves in the right language, the same placeholders in each, every `error=` in `auth.py` a catalogue call, every key reachable *and* used, no template printing a pair raw), plus `test_the_translated_list_only_grows_with_intent` pinning the exact count — it was a floor of 6, which passed while a page was deleted from the list, turning the sweep off for that page silently. Mutation-checked: 7 of 7 injected defects caught (a route back to a literal, a bare `_classify_login_error` return, an untranslated English half, `{{ error }}` in a template, the macro choosing a language server-side, the API boundary removed, a page dropped from the contract)
- **The exam builder is the largest translated page and the one with the most JavaScript.** `teacher/exam_form.html` (1054 lines, the page a teacher spends the most time in) carries ~144 `t()` pairs across the form, the AI upload path, the question manager, the anti-cheat card and the sidebar. Two things there need the escape hatches rather than the pair: a message a *script* builds and shows once (the loading line, an `alert()`, the `confirm()` on delete) goes through `sgT()`, because a string assembled outside a template is frozen in the language it was built in and nothing re-evaluates it; and the page's own `<h1>` and its option labels bind a Jinja-computed value into `t('{{ … }} Ujian','{{ … }} Exam')`, which the sweep still reads correctly because Jinja is blanked before the literals are read. `student/take_exam.html` needs the third mechanism — `relabelLocale()` driven by `$watch('lang')` — because it *stores* labels in component state, which no binding can re-evaluate. Guard: the page is on `TRANSLATED`
- 4 standalone documents do NOT inherit base.html and are hardcoded Indonesian (`lang="id"`): `monitor.html`, `print/report_card.html`, `student/result_detail_pdf.html`, `teacher/print_exam_report.html`. They cannot honour the English default
- Color theme: `primary` (blue) defined in Tailwind config — `brand-*` was previously undefined (invisible buttons/text), now aliased to `primary` palette
- No direct DB DDL access — migrations must be run manually in Supabase SQL Editor
- `profiles` table columns: `['id', 'full_name', 'phone', 'role', 'created_at', 'updated_at', 'nisn', 'nis', 'class_id', 'school_id', 'tz_offset']` (migrations 002+013 applied)
- `schools` table exists in `public` schema (migration 013 applied)
- `school_settings` table (legacy, single-row INT id=1) exists alongside `schools` (UUID id)
- `submissions` table: `started_at` column added (migration 013); `is_hidden`, `retracted` status added (migration 003); unique constraint `(student_id, exam_id)` added (migration 013)
- `pengumuman.school_id` is currently INT referencing `school_settings(id)` — migration 024 fixes to UUID
- MCQ answer keys: single value (`"A"`), multiple (`["A","B"]`), or `"bonus"` (all correct)
- Weighted scoring: Teacher sets MCQ% + Essay% (total must = 100), distributed equally per question within each group
- `question_weights` JSONB column exists on `exams` table (migration 003 applied)
- Anti-cheat columns (11 columns) exist on `exams` table (migration 011 applied)
- PDF generation uses `xhtml2pdf` (pure Python) — WeasyPrint requires GTK/Pango not available on Windows
- Anti-cheat: graduated penalty (1st=warning, 2nd=-base, 3rd=-2×base, 4th+=-3×base), auto-submit on max violations
- Router / security: 404 catch-all `/tools` for admin functions; CSP `frame-ancestors 'self'` in response headers
- **Session timeouts** (OWASP + UU PDP): super_admin idle=15m/abs=4h, admin_sekolah=30m/8h, guru=60m/12h, murid=120m/24h
- **The deploy runner is never a copy**: `/usr/local/bin/scangrade-deploy` and `/usr/local/bin/scangrade-db-snapshot` are `deploy/entrypoint.sh` rendered with the checkout path, and they exec `deploy/*.sh` **in the checkout** (chosen by the name they were installed under). `install-auto-deploy.sh` used to `install` the scripts *to* those paths, which is a snapshot: fixes to the deploy logic then sat on GitHub while root kept running the version of the day it was installed — and the installed `scangrade-db-snapshot` was broken outright (the wrapper derives the checkout from its own location, so it looked for `/usr/local/.venv/bin/python` and refused to snapshot). Gate 0 of `deploy/scangrade-deploy.sh` now compares the file it is running with `deploy/scangrade-deploy.sh` in the checkout **before fetching or merging** and exits **14** when they differ (a byte-identical copy passes); it sits after the pause check on purpose. Guards: `tests/unit/test_auto_deploy.py`, which also renders the launcher and runs it — changing the checkout's script changes what the installed path runs, arguments are dropped for the deploy and forwarded for the snapshot. **One-time migration**: a box installed before this change keeps working but still runs its old copy; `sudo bash /opt/scangrade/deploy/install-auto-deploy.sh` once, and it cannot recur
- **The installed launcher re-renders itself on a successful release, so it cannot lag its template — but a *drifted copy* still needs one root install, and saying otherwise would be the overclaim this repository keeps catching.** The last step of a release that passed every gate rewrites `/usr/local/bin/scangrade-deploy` **and** `/usr/local/bin/scangrade-db-snapshot` from `deploy/entrypoint.sh` in the checkout it just verified (`refresh-launcher-logic` block), so from that release on what the timer runs *is* the checkout's launcher and a fix to the arrangement arrives on the next tick with nobody at a console. Three properties, because the installed path is the only thing the timer runs: it **parses before it installs** (the `@REPO@` placeholder is checked and `bash -n` runs on the staged render *before* the `mv` — a launcher that cannot run is strictly worse than a stale one, since then nothing can deploy at all, and every failure leaves the installed file exactly as it was); the install is **atomic** (staged in the target's own directory, so `mv` is a rename on one filesystem and no tick can observe a half-written file); and it is **skipped when the bytes match**, because Gate 0 compares *content*, so an mtime that moved with no content change would be a signal that lies. It **cannot take a release down with it** — the app is verified and serving by then, so a failure is logged and nothing else happens, and putting `HEALTHY=0`/`FAIL_REASON`/`quarantine_write` in this block would trade a working site for a bookkeeping fix (and quarantine a good commit) — and it does **not** run on the rollback path, where the checkout is about to move back and a launcher installed there would point at logic that no longer runs. The helper reads its target through a global (`LAUNCHER_TARGET`) rather than a parameter, the same choice the quarantine block makes, because `test_deploy_script_takes_no_arguments` forbids reading root's argv *anywhere* in that file. Guards: `tests/unit/test_auto_deploy.py` — seven source assertions (delimiters, parse-before-install order, staging beside the target, the match skip, the forbidden lines, the runner's two paths matching what the installer installs, and the single call site inside the `HEALTHY=1` branch) — read over `_refresh_code()`, the block with its comments removed, because a guard that reads prose fires on a sentence: the block's own header explains that a drifted copy still refuses with `exit 14`, and "exit 1" is a substring of that, so the forbidden-words check lit up on a comment and invited a relaxed guard instead of a correct one plus a behavioural harness that runs the real block: a drifted copy is replaced by exactly what the checkout renders, the second run changes nothing (the mtime is the tell), a missing snapshot launcher is created, a render that does not parse leaves the working one alone, a failed `mv` is reported and not fatal, and end-to-end a **stale launcher is re-rendered on the next release** — running it first goes *through* Gate 0 to the checkout's script with zero arguments forwarded (which is what makes it reachable, and why the staleness has no symptom), and after the refresh the installed bytes equal what the checkout renders. The limit is a test too: a drifted copy is refused with exit 14, and an assertion pins that the refusal's `exit` precedes the only call site, so no test can pretend the heal happens there. Mutation-checked, **13/13 injected defects caught** (`.freebuff/mutate_launcher_refresh.py`, which round-trips the runner as bytes for the line-ending reason below). **This change broke the status page before it fixed anything, and the suite said so.** `/super-admin/deploy-status` decided "is this a launcher?" with a bare `'@REPO@' in text`, and the refresh block names `@REPO@` three times — so a **copy of the runner** was read as an *unrendered launcher* and the reading returned before the byte comparison the page exists to give; twelve `test_deploy_status.py` tests went red at once. The reader now keys on the placeholder's **assignment** (`UNRENDERED = 'REPO="@REPO@"'`, `deploy_status_service.py`) because a file that merely mentions the token is not an unrendered launcher and a file that assigns it is — and a regression test writes a copy of the real runner and demands `kind == "copy"` (it asserts the runner still contains the token, so the fixture cannot silently stop reproducing the shape). The lesson is the coupling, not the constant: the runner's own text is *read* by the app, so a new mention of a token in one file changes the meaning of another. **What it heals, exactly.** A launcher rendered from an older `deploy/entrypoint.sh` (still runs the checkout, so nothing is broken, but the template's fix is not in effect — the page calls it `launcher_stale`); a missing launcher or snapshot command; and a copy that still matches the checkout **byte-for-byte**, which Gate 0 lets through precisely because it is the same code. **What it cannot heal:** a copy that has *drifted* — the state `exit 14` names, and the likeliest state of an old box. That copy refuses before any gate can pass, and the refresh is the last step of a release that passes, so no release reaches it; nothing inside a file that is not the checkout's can apply the checkout's logic, which is the one step a file cannot take for itself. A copy that predates Gate 0 is stuck the same way (it has no check to fire, so it keeps deploying with the logic of the day it was installed). Both are one `sudo bash /opt/scangrade/deploy/install-auto-deploy.sh` and cannot recur afterwards. Honest limit: a hand `git reset --hard` is not a release, so it refreshes nothing.
- **The wrong way to arm a box looks exactly like success, so `deploy/arm-auto-deploy.sh` says out loud what the box is running.** `install-auto-deploy.sh` needs root (it writes the two launchers and the units), and run from a non-root shell it prints a banner and exits **2 before touching anything** — while the timer keeps ticking, the site keeps serving and the journal stays quiet, so "I ran the installer" stays true of a box that is still running a **copy** of the deploy script from some earlier day: it deploys every release with no theme, claims, performance or quarantine block in it, has no `/etc/scangrade-claims.conf`, no `/etc/scangrade-perf.conf` and no roster, and nothing on the box goes red. That is what happened here: `/usr/local/bin/scangrade-deploy` was the 12 September copy (all five markers absent), `auth.log` recorded **no privileged command at all** for the day, and the state the operator reported as installed was measurably not. The wrapper is one command that removes the ambiguity: it prints what it is running *before* and *after* (`bash deploy/arm-auto-deploy.sh [--check]`), elevates once with sudo/doas when it has to, places the load-test roster, keeps every line of the installer in `/tmp/installer.log` and passes its exit code through. It reads the installed file the way `/super-admin/deploy-status` does — a launcher that execs the checkout (armed), `@REPO@` still assigned (installed but never rendered, so nothing was ever deployed by it), or anything else (**a copy**, whose missing gate blocks are printed by name so "no theme gate, no claims gate, no performance gate, no quarantine" is a sentence rather than a conclusion to reach) — and `--check` exits **0 only when all four gates have what they need**, naming each missing launcher, conf or roster otherwise; it writes nothing at all, which a test asserts byte-for-byte. Three refusals exist because each is a way the real attempt failed: it will not start a password prompt without a terminal (that run hangs unanswered), it will not loop when `SG_ELEVATED` shows the privileged shell did not take, and it reads the roster **before** asking for anything — a roster the gates cannot draw from is worth reporting without making anyone retype a password, and it is never installed (one account per session, owned by the service user and mode `0600`, because the installer's `as_owner` runs the gates as that user, not as root). The paths are env-overridable so the tests can drive the real script against a scratch tree; the roster's *destination* deliberately is not, because that is the path the installer and both gates already agree on. Guard: `tests/unit/test_arm_auto_deploy.py` (17 tests, mutation-checked **10/10**, `.freebuff/mutate_arm_auto_deploy.py`)
- **An exam runs on two clocks, and the teacher chooses which one ends a sitting.** `exams.end_at` (migration `030_exam_window.sql`) is the **assignment window end**: the last instant a student may *begin* — after it the exam leaves the student's list, while whoever already began keeps working, because a window governs beginning and not finishing (a sitting still open therefore stays on the list after the window shuts, via `draft_ids` in `student.py`). `exams.duration_minutes` is the second clock: counted from *that student's* `started_at` and allowed to run past `end_at`, which is what the teacher form has always said on the field. `exams.auto_submit_on_window_end` swaps them — the sitting then ends at `end_at` whatever time it started, i.e. the **earlier** of `end_at` and `started_at + duration` ("regardless of when they start" cannot mean "longer than the duration"). `app/utils/exam_window.py` is the one place both are read: `window_state`/`may_begin` (the door), `deadline`/`deadline_reason`/`seconds_left` (the countdown), `is_late` (the record). It exists because four callers answering separately is how a page tells a student "12 minutes left" while the server treats the paper as finished — the student list and dashboard (`_offerable`), the door into an exam, `/api/student/sync-draft` (which counts from the **stored** `started_at`, never the client's claim: a client that could move the origin could extend its own exam) and the submit route, which records `submissions.submitted_late` rather than refusing — a phone that loses its network in the last minute must not lose the answers, but "this paper was timed" and "this paper arrived twenty minutes after the deadline" are different facts about a result. The teacher form gained the field, the toggle and the note that the duration may exceed the window; `end_at` is written by both the create and the update handler, and a window end at or before the start is refused rather than stored. **The form's anti-cheat half had two real defects**, both of the kind that make a teacher's setting silently not stick: the "Question settings" card opened its own Alpine scope with `randomize_questions: true, randomize_options: true` **hard-coded**, shadowing the stored values — an exam saved with shuffling OFF rendered ON and the next save wrote `true` back — and `block_screenshot` was read by the route with no control anywhere on the page, so it could only ever be its default. Both are fixed, and `tests/unit/test_exam_window.py` fails if any checkbox the form posts is a name no teacher route reads, since an unchecked box posts nothing at all and the route's default is the off state. Guards: `tests/unit/test_exam_window.py` (31 tests) and `tests/unit/test_exam_assignment.py` — which caught the one real bug in this change: `draft_ids` was computed after the list filter that needed it, so every exam vanished with `cannot access local variable 'draft_ids'`
- **A recorded fact has to be shown where it is used, and the mark reads the record rather than recomputing it.** `submitted_late` was written and displayed nowhere, so telling a timed paper from one that arrived twenty minutes late required opening the database — the one thing a marking screen exists to prevent. Three surfaces carry it now: the results list (both halves of `teacher/_results_table.html`, so the mobile card and the desktop row cannot drift apart), the marking page beside the student's own name, and the printed roster on the sheet a school files. `_exam_results` counts them into `stats['late']` in the same pass as the other statistics, from the same column the rows are marked from, and the results header shows the count next to the participant count — a badge on row 180 of 500 is a badge nobody scrolls to. **The mark reads the stored column and never `exam_window.is_late`**: the deadline is arithmetic over `end_at`/`duration_minutes`/`started_at`, all still editable, so a teacher who lengthened the duration after the sitting would erase the evidence that a paper was late. Two conventions come from the gates rather than taste: the label in the results list and its partial is a `t()` pair, because a partial whose copy is all pairs cannot gain a literal without dropping below its coverage floor; and the marking page writes it as **plain Indonesian**, because that page declares `content_lang = 'id'` and `deploy/i18n_coverage.py` refuses a page that newly pins its language while carrying pairs — a pair there would be the one string that switched, leaving English words under an Indonesian `lang`. Guard: `tests/unit/test_late_papers.py` (20 tests) — the pages are **rendered**, not grepped, a late row and an on-time row together, with the pair counted once per half so a label added to one table and forgotten in the other fails — mutation-checked, **14/14 injected defects caught** (`.freebuff/mutate_late_papers.py`), the one survivor being exactly that: a check for "a pair appears" was satisfied by the half that still had it, and is now an equality against the number of marks. `deploy/i18n_baseline.json` re-recorded for the two templates that gained pairs (`_results_table.html` 7 → 11, `results.html` 3 → 5)
- **A scanned paper answers the late question through a second function, because a paper exam has only one of the two clocks.** `exam_window.late_arrival(exam, sitting_started, arrived_at, stated)` is the same arithmetic as `is_late`, asked by the one caller that cannot see the other end: an online submission's arrival and its `started_at` are the same session, while a scan's recorded time is when the *teacher saved it* — so the mark is computed from the student's own sitting when the app saw one, else the exam's `start_at` (for a paper exam, when the papers went out), else no deadline at all, and the **teacher's own answer wins outright** when the request carries one. Only `/api/scan/save` and `/api/scan/bulk-save` use it; the online submit route keeps asking `is_late` directly, because routing it through `late_arrival` would let a teacher's mark override a session that was actually timed. Two silent-failure shapes are closed by construction: **the bulk select names every window column**, since PostgREST reads a column left out of a select as *absent* and never as an error — a forgotten `end_at` would mean "never late" for a whole class with nothing in any log — and **a later page never clears an earlier one's mark** (`if stated_late is None and stored_late: late = True`), because page 2 arriving on time is no evidence about page 1. The correction is a teacher's route (`POST /teacher/submission/<id>/late`, ownership-guarded by `_guard_exam`) that writes **one boolean and nothing else**, so a graded or published sheet can be corrected without being re-graded; the control sits in both halves of `_results_table.html` beside the mark it changes, because thirty rows at once is where a wrong mark is *seen*. The box on the scan screen sends a value **only when ticked** (`late: … ? true : undefined` — absent means "let the clock decide", and a box that always posted false would assert that every paper was on time), and it is cleared both where the save button appears and in `resetScan()`, or one teacher's tick follows the next pupil's paper. Guards: `tests/unit/test_late_scans.py` (30 tests) — the rule's three clocks and the teacher's override as behaviour, the two routes and both payloads as source, the correction control **rendered** once per half of the table — mutation-checked, **25/25 injected defects caught** (`.freebuff/mutate_late_scans.py`). All six first-round survivors were real gaps; the instructive one was a slice that ran from the single save's `fetch` to the *bulk* request, so it stayed satisfied by the response-handling `data.late` after the payload itself had been emptied.
- **A marked paper has two halves, and only one of them was on a screen — the other was sitting unread in `submissions.answers`.** Every teacher view answers "who scored what"; none answered what each *question* did, which is the half that tells a teacher which question measured nothing, which one the strong half missed, and which distractor nobody chose. `app/services/item_analysis.py` reads the answers the sheet already collected and reports both families: **classical** item statistics (difficulty as the share of the marks a question actually paid out — routed through the app's own `question_types.part_factor`, so partial credit for a matching or ordering question counts here exactly as it counted on the paper; `D` from the upper 27% against the lower 27%; the point-biserial against the rest of the paper; Cronbach alpha, KR-20 and SEM) and a **Rasch calibration** in the Winsteps family (item difficulty and student ability on one **logit** scale, each item's S.E. and its `t = (M1 - M2) / sqrt(SE1² + SE2²)` with Welch–Satterthwaite degrees of freedom — Winsteps' own definition, `www.winsteps.com/winman/t-statistics.htm` — with the two-sided probability computed through the incomplete beta function so the 1.96 / 2.58 thresholds mean what that table says they mean; infit/outfit MNSQ against a 0.5–1.5 band; person and item separation and reliability; and a bin distribution for the Wright map). It deliberately borrows rather than restates: an essay's score share is the teacher's mark in `teacher_feedback.scores`, and a paper's marks come from `question_weights`, so "difficulty" is measured against what a question was worth and not against an equal split. **Three limits are printed on the page rather than hidden in the module**: items are calibrated **dichotomously** (a part-credited matching question is a polytomous item and the Rasch model for it is the rating scale model, which this is not — the classical columns do use the part credit, so the two halves answer slightly different questions); a question with **no answer key** is reported as unkeyed rather than scored wrong, even though the app's own grader does score it wrong, because "nobody answered" and "there is no key" are different findings; and a blank is **missing, not wrong**, so it leaves that item's statistics instead of counting as a failure. **Two real defects in my own calibration were caught by probing, not by a test**: extreme response patterns were first mapped straight from a proportion onto the scale, which published a perfect paper at the *bottom* of the ability scale and a blank one at the top — the order inverted, and every test that only asked "is it a float" passed; an extreme pattern now nudges the raw score by 0.3 score points and solves the **same** equation the finite papers get, and the solver is bisection on the monotone equation rather than Newton, which diverges on Guttman-like patterns. `app/services/analysis_report.py` turns the same `Analysis` object the screen renders into a **CSV** (sortable, with a BOM so Excel reads it as UTF-8 and an accented name is not mojibake) and a **PDF** for the exam file, each carrying a per-language label table keyed by name rather than two dicts — so a missing translation is a `KeyError` here and not an Indonesian word on a page that said it was English — and the download links carry the reader's own language, because a hard-coded `lang=id` filed an Indonesian report for an English reader. `/teacher/analysis/<exam_id>` renders four surfaces (difficulty and discrimination per item, the logit ruler, the score distribution, distractor bars) and is reached from a button on the results screen; the chart payload is built server-side in `_chart_payload` rather than by Jinja loops assembling arrays inside an `x-data` attribute, which is untestable and quietly drops a question that has no measure. **Two defects the new numbers exposed in code that was already there**: the legacy 70/30 fallback in `question_types.default_weights` divided each pool independently and rounded, so three essays were worth 23.33 each and a perfect legacy paper scored **99.99** — a paper that could never reach 100, which `min(earned, 100)` cannot repair because the number is *below* the cap (it now re-bases the whole paper in tenths through `mark_scheme.normalise_to_100`, keeping the 70/30 ratio and making the parts add up to the paper's own total); and `guide/skor.html` still documented the old MCQ/Esai pool model for a release after it changed, so its penalty table is now **computed** from `calculate_graduated_penalty`, the same function that charges a student, with the settings it documents stated explicitly — because copying the numbers into the template is how that page drifted in the first place. `teacher/grading_queue.html` came with it: a fixed `w-72` queue beside the marking panel left roughly 100 px of paper on a phone, and it is a column there now. Guard: `tests/unit/test_item_analysis.py` (30 tests) and `tests/unit/test_analysis_page.py` (35) — the pages are **rendered**, not grepped, the guide's table is compared against the service that charges the penalty rather than against a copy, and the door to another school's exam is one function a mutation can open — mutation-checked, **33/33 injected defects caught** (`.freebuff/mutate_item_analysis.py`). `deploy/i18n_baseline.json` re-recorded: `teacher/analysis.html` at 105 pairs and 100%, `teacher/results.html` 3 → 6, and `guide/skor.html` leftovers 58 → 102, which is that guide gaining the copy it was missing rather than losing a translation.
- **A green suite cannot see a browser console, and both defects that lived there made a page render nothing and a chart draw twice.** They were found by watching `/teacher/analysis` in a live preview *after* the full suite passed. **The apostrophe:** `t('id text','English text')` lives inside an HTML attribute, so `&#39;` is decoded to `'` **before** Alpine evaluates the expression — the English string ended at `class's`, Alpine raised `missing ) after argument list`, and the span rendered **empty in both languages** (two sites: the page subtitle and a metric's hint). `&quot;` is the opposite case and must *not* be flagged: a double quote inside a single-quoted JS string is legal, and it is how a template writes a quoted word in an attribute that cannot hold quotes itself ("Export My Data" in `student/settings.html`, the API-key steps in `teacher/ai_settings.html`). **The double init:** `x-data="itemAnalysis(…)" x-init="init()"` runs the component's `init()` twice, because Alpine calls a component's own `init()` method already — harmless for most components and fatal for one that creates a Chart.js chart, which threw `Canvas is already in use. Chart with ID '0' must be destroyed before…` on the second chart, and registered the language watcher twice so a single toggle redrew everything twice. The attribute is gone, and the page's own render test now asserts its absence. Guard: `tests/unit/test_alpine_expression_safety.py` decodes every attribute the way the browser does and requires each `t()` call to survive that decoding (flagged by an apostrophe entity or by a raw contraction), and requires a template that calls `new Chart(` not to pair `x-data="component(…)"` with `x-init="init()"`. It is pointed at the real broken text so it cannot decay into a comment, and it deliberately passes the three legal shapes that made a stricter first version unusable: a pair built by concatenation, a pair whose text comes from Jinja, and an escaped apostrophe.
- **A legend that only exists inside the copy buffer is a legend the reader of the page never sees, and a number with no definition is a number the reader guesses at.** The statistics page grew its numbers faster than its explanations: an item table, a separation block, an upper-and-lower table, an option spread and three charts, with the definitions living in the *paste* — a copied table carried its column key, the page it was copied from did not. Every block that publishes numbers now carries its own legend **on the page**, and the page and the paste are built by the *same function* (`chartLegend()`, `tableLegend()`, `summaryLegend()`, `splitLegend()`), so the two cannot drift — an on-page list of sentences that is a copy of the pasted one is the copy nobody notices going stale. The item table's columns are named by `data-col` and defined from that key, so a column whose definition is missing cannot travel into a report as a bare letter; the two tables that had only a one-line caption (upper-and-lower groups, and the option spread) now have their own — the split table's five columns and its legend are one list, and the option key uses the very utilities the bars paint with, so a legend cannot promise a colour the chart does not use. **Three defects found while verifying in a live preview, none of which the suite could see**: (1) the browser tab read `<span x-text="t('Analisis Butir Soal'…` — a `<span>` inside `<title>` is not rendered, it is *read out verbatim* — on `teacher/analysis.html` and `teacher/analytics.html`, both now plain text in the block with the pair moved to an `x-effect` that sets `document.title` after the toggle exists (measured live: *Analisis Butir Soal - Fisika* / *Item Analysis - Fisika*); (2) the students card printed "1 Papers with data" beside an exam with one paper, and the copied summary — which reads the same `hint` — copied it, so one paper now gets the singular; (3) the composed figure was checked by decoding its pixels rather than by trusting the canvas: 1080×2072 with ink from y=33 to y=2029, i.e. the legend is inside the image and nothing is clipped at the edge. Guards: `TestTheMeaningIsOnThePage` in `tests/unit/test_analysis_page.py` (the binding is asserted, not the method name — a legend deleted from the page still *exists* as a method nobody calls, which is exactly how the first version of these tests passed a legend that had been removed) and `TestTheTitleIsText` in `tests/unit/test_content_lang.py`, which is app-wide: no `{% block title %}` anywhere may contain markup. Mutation-checked, **6/6 injected defects caught** (`.freebuff/mutate_meaning_legends.py`) plus **2/2** for the title guard.
- **A school that had not yet run a paper was told its answer key was missing.** `/teacher/analytics`' totals line counts exams "without a key", and it counted *every exam with a hole* — `holes` is `unkeyed` **or** `unscored`, and a paper nobody submitted to has one hole per question. The two were indistinguishable because `item_analysis.analyse(exam, [])` raised on the empty matrix, so the route's `except` branch zeroed the count and both uninhabited exams came out at zero for the wrong reason; once the empty case became a supported state, the same exams came back as four missing keys each. The row now carries `unkeyed` separately and the total counts that, so "no key" means the key: the paper nobody sat is unmeasurable and says so under `holes`, which is a different sentence to a teacher. Guard: `test_a_paper_nobody_sat_is_not_a_paper_without_a_key` in `tests/unit/test_analytics_scope.py` — it asserts the fixture still holds an exam with a key and no submissions, which is the state a crash in `analyse()` used to hide. Mutation-checked: folding the count back to `holes` fails **3** tests.
- **gunicorn is not the only process holding the release, and nothing probes the other one.** The Celery worker imports its task modules at start-up and keeps them in memory for the life of the process, so `reload_app` leaves it answering with the *previous* release — and because the worker is not what the health probe calls, the deploy reports success and the site looks healthy while every async task runs old code. It is not theoretical: `d44ab20` added `page_index` to `process_omr_scan` **and** to its caller in `omr_tasks.py`/`api.py`, the four deploys after it reloaded gunicorn alone, and every teacher scan failed with `process_omr_scan() got an unexpected keyword argument 'page_index'` — the caller new, the worker old, and nothing on the box saying so. A task *signature* is the sharpest form of it (Celery serialises kwargs by name, so the worker rejects an argument the web process just started sending), but any change to a task module has the same shape: the worker is a snapshot taken at its own start-up, not at the last deploy. `deploy/scangrade-deploy.sh` now restarts `WORKER_UNIT` on the forward path (Celery has no SIGHUP reload) *and* on the rollback path, since leaving it on a rejected revision is the same mismatch in the direction a rollback hides best — the site is up while background work fails. A unit that is not installed is not a failure (async OMR simply queues) and a restart that fails is **not** a rollback (the app is healthy; rolling back would turn a broken worker into an outage), but it is not a footnote either: it goes in the journal as a warning naming the fix, because a worker that quietly stays on the old code is exactly the state that broke scans. Guard: `tests/unit/test_auto_deploy.py` — `test_the_deploy_restarts_the_worker_that_shares_the_checkout`, `test_the_worker_is_restarted_on_the_forward_path_not_only_on_rollback`, `test_the_deploy_reloads_the_worker_on_the_way_back_too`, `test_an_unrestarted_worker_is_reported_at_the_end_of_the_deploy` and `test_the_worker_unit_runs_this_app_from_the_checkout` check the restart is on **both** paths, that the two are *not* the same edit (a rollback that skips the worker, and a forward path that names a unit no installer creates, are different defects), that ordering puts it after `reload_app`, and that the worker is this app's own checkout — mutation-checked, **9/9 injected defects caught**
- **A rollback moves the checkout, not the branch — so a refused release used to come back every two minutes.** Every gate below rejects a release with `git reset --hard` to the previous commit, which is correct and incomplete: `origin/main` does not move, so the next tick fetched the same commit, saw it was not what was deployed, pulled it, and walked into the same gate. Rejected, rolled back, re-pulled — a box that reloads gunicorn **and the Celery worker** forever while the journal fills with one repeated failure, and the only exit is a human pushing a fix. Nothing about it is visible on a page: the previous release serves throughout. `deploy/scangrade-deploy.sh` now **quarantines the commit**: `quarantine_write` records the full sha, when, and which gate refused it to `/var/lib/scangrade-deploy/quarantined`, and `quarantine_gate` makes the next tick say why and `exit 0` without touching the checkout. It lifts itself the moment `origin/main` moves — a new commit is a new release, and the refused one is no longer the question — and there is one deliberate escape hatch, `touch /etc/scangrade-deploy.release`, **consumed on use** so it cannot become a standing override that quietly pins a known-bad commit. Three failures are **not** quarantined because they are properties of the box, not of the release, and retrying is correct: a failed `git fetch`, a snapshot that could not be taken for a migration release (nothing is merged at that point), and a `pip install` that failed (usually the network). Only a **gate** quarantines — `compileall`, app construction, the theme gate, and the post-reload verification (smoke, claims, perf, the app not answering `200`) — and the record names which, so "why did nothing deploy" is one `cat`. Reading globals rather than taking arguments is deliberate: `test_deploy_script_takes_no_arguments` forbids a positional parameter anywhere in the file, and reading `AFTER_FULL` — set once from `origin/$BRANCH` — also removes any chance of quarantining a commit other than the one a gate actually refused. A **hand** `git reset` writes no quarantine, so after one the next tick does try again; that is stated in `docs/AUTO_DEPLOY.md` rather than papered over. Guard: `tests/unit/test_auto_deploy.py` — source assertions for the skip, the auto-lift and the escape hatch, plus a behavioural harness that runs the real `quarantine-logic` block extracted between its delimiters (the arrangement Gate 0 already uses): a first call passes, the same commit then skips, a different one lifts the file, and the release file is honoured and consumed
- **A line ending git refuses to talk about can still make a byte comparison lie.** `.gitattributes` declares `eol=lf` for `*.sh`, `*.py`, `*.md` and the git hooks — so a fresh checkout and the Linux box always get LF — but with `core.autocrlf=true` **`git status` will never show a CRLF working tree**, because the CRs are normalised away on the way into the index. Nothing complains until something compares *bytes*: Gate 0's `cmp -s`, and `test_deploy_status.py::test_a_line_ending_difference_is_a_difference`, whose whole job is to prove `cmp` does not normalise — and a repo file that is *already* CRLF makes the CRLF copy it creates byte-identical, so the guard that should catch drift reports a match. A scratch harness of mine did exactly that (it read the runner with `read_text` and wrote it back with `write_text`, which translates `\n` to the platform separator, converting all 543 lines at once), the deploy tests went red while `git diff` stayed a clean 48-insertion diff, and the file looked untouched. `test_the_scripts_compared_as_bytes_are_lf_on_disk` now asserts `b"\r\n" not in read_bytes()` for the three files that are read as bytes (`scangrade-deploy.sh`, `entrypoint.sh`, `scangrade-db-snapshot.sh`), naming the two reasons it is invisible — the tool that did it and the config that hid it — because the next person to see it will otherwise chase the diff. Only those three are guarded on purpose: 80 tracked files in this checkout are CRLF merely because they predate `.gitattributes`, which is cosmetic, and a guard over all of them would be noise. Note this is the same defect the i18n baseline already carries a paragraph about (`write_text` translating `\n` on Windows, and a LF-only baseline committed by hand) — the lesson keeps arriving through a different door each time: **on Windows, a Python tool that round-trips a text file is a line-ending tool.**
- **An old runner is not refused — it is obeyed, which is why the page had to name it.** `/super-admin/deploy-status` (`deploy_status_service.py`, route in `super_admin.py`) reads the installed runner and the checkout without SSH, and its first version reported production's state with the wrong consequence: the box's `/usr/local/bin/scangrade-deploy` was the revision from **before Gate 0 existed**, so there was no check in the file that runs to refuse anything, and releases had been going out for five days with the deploy logic of **90a01c7** — **45 commits behind**, **15 files under `deploy/` since** — while the page said *"Gate 0 refuses it with exit 14, so no release can deploy"*. The site looked healthy because it was: the failure was that every gate built after that commit was absent. So the consequence is now derived from the copy itself (`IDENTITY_MARKER in text`): a copy that carries the check is `copy_drifted` (Gate 0 refuses it, nothing deploys), and one that does not is `copy_predates_gate` — the quieter failure, and the one to act on. The installed file is **named, not merely called stale**: its bytes are hashed as a git blob and `git log --all --find-object` is asked which commit holds them — then each candidate is verified with `cat-file`, because `--find-object` also reports the commit that **removed** the blob (the first version took the first line, named the newest commit, and reported a runner one commit behind as being *zero* behind); a launcher is matched by rendering each of the last 25 `entrypoint.sh` revisions. The distance rides on the page (`Commits behind (runner)` leads the numbers, above the checkout's own), and `cat-file` output must not be `.strip()`ed — `_git_out` trims, so a dedicated `_git_raw` exists: trimming drops the trailing newline the installed file has and every stale launcher then reads as *unmatchable* rather than old. Verified against production's exact shape rebuilt from real history in a throwaway clone: the fingerprint the service computes (`a1af616ecbe5`) matches the one the live page read off the box. Guards: `tests/unit/test_deploy_status.py` (53 tests) — including the two corrections above, and the regression that renders the page for a runner older than Gate 0 and asserts the refusal sentence is **absent**, and that it is still present for a copy that carries the check
- **A runner that cannot deploy has no symptom, so a page has to show it.** `/super-admin/deploy-status` (`app/services/deploy_status_service.py`, route in `super_admin.py`, and a sidebar entry) reports whether `/usr/local/bin/scangrade-deploy` is the checkout's launcher or an installed copy, and how far the checkout is behind `origin/main` — read-only, no shell. Three states, only one of them correct: a launcher rendered from this commit (nothing to do); a launcher rendered from an *older* `deploy/entrypoint.sh` (it still runs the checkout, so nothing is broken, but whatever changed is not in effect); and a **copy** — the arrangement the launcher change replaced, where Gate 0 lets a byte-matching one through and refuses a drifted one with **exit 14**, so *nothing deploys again* while the site keeps serving and no page changes anywhere. That last state is the whole reason the page exists. The copy is compared as **bytes**, exactly as `cmp -s` in Gate 0 does, because comparing text normalises line endings and the first version called a copy a match that the gate then refused — the test that runs the real `runner-identity` block over the same files caught it; the launcher half is a text comparison against what `entrypoint.sh` renders, which is a different question (`_normalise_exam_json`'s sibling lesson: two readers of one fact drift apart silently). Every reading is a stable **key** plus its data — a path, a git error, a count — and the sentence lives in the template in both languages, because prose built in Python is copy the toggle cannot reach and the i18n sweep cannot see. Nothing fetches: `origin/main` travels with the age of the reading, and every git call carries `--no-optional-locks` since a plain `git status` may rewrite the index on the checkout the deploy is about to use (an allowlist test fails on a new subcommand rather than a blocklist excusing one). A part that cannot be measured reports a reason instead of a zero, because a zero here reads as "up to date". Guard: `tests/unit/test_deploy_status.py` (38 tests — the three-file agreement with Gate 0, a line-ending difference the page must call a difference, the reading that does not touch `.git/index`, and every verdict key having a sentence)
- **A gate that compares a release with the last one that passed, because a claim gate cannot see slow drift.** `deploy/claims_gate.py` compares this box with a number printed on the landing page, at the rung the page advertises — so a release that costs 40% of every page's response time passes it, and five of those in a row are a box that no longer does what it did. `deploy/perf_gate.py` (Gate 6, after the reload, before the release is declared good) runs a **small fixed reference load — 20 students / 20 s** — and compares it with the last release that passed: same harness, same box, same load, so the two numbers cancel and what is left is what the release changed. Small on purpose, and for the same reason the claims gate caps itself at 60: this is the 1 vCPU that serves students. It reuses the claims gate's probe, identity checks and two-strike verdict rather than reimplementing them. **The baseline is written only when a release passes** — a refused release must never become the yardstick, or the regression becomes permanent and invisible; a deliberate slowdown is recorded with `--rebaseline` instead. Exit codes are the claims gate's (0 pass · 1 confirmed regression, rolls back only when `PERF_ENFORCE=true` · 2 could not measure, **never** a rollback: no baseline yet, a changed reference load, a busy box, an unconfirmed divergence). Evidence: `/var/lib/scangrade-deploy/perf/{baseline.json,history.jsonl}`. Guard: `tests/unit/test_perf_gate.py`
- **A page can get more expensive without getting slower, which is what the clock alone cannot see.** Response time is a *symptom*, and at the 20-session reference load this box is far from saturated: a release that adds three queries to the student dashboard, or lands a 200 KB script on it, still answers inside the latency slack — and is then the page that falls over at 500 students. So every request counts the Supabase round-trips it spends and says so on the response, `X-Supabase-Roundtrips: 7` (`app/utils/query_meter.py`, armed in `before_request` ahead of every other hook, counted per **attempt** in `app/utils/supabase_retry.py` so a retry costs two, and scoped to the greenlet/thread serving the request). `loadtest_concurrent.py` records that header and each response's byte size, and Gate 6 compares two more numbers against the last release that passed: the heaviest page's payload (1.25x **and** +8 KiB) and the busiest page's queries (1.25x **and** +1), each on its own worst page, both keeping the same two-strike confirmation as latency. The ratio catches a real multiple and the grace forgives a list that honestly got longer; a release has to clear both. **Two things here were measured rather than reasoned about, and both changed the design.** (1) `/student/dashboard` reported **0 queries on 12 of 12 requests in 14 seconds** — `cache_get` served every one, because the 30-second entry was warmed by the login redirect — so the gate would have been blind to the busiest page a student has. A cache hit now replays what the entry cost to build (`app/utils/cache.py` remembers it at `cache_set`, and `cache_delete` drops it with the value, or a key rebuilt where no meter is armed would keep reporting an entry that no longer exists). The header therefore answers *what this page costs when it does its work* — identical warm or cold — where "what this request spent" would have been a measurement of the cache instead of the release. (2) The device's own `__pycache__` made three mutations invisible: a `.pyc` is invalidated on `(mtime, size)` with the mtime stored as **whole seconds**, so `max` → `min` — same length, same second — was read from the cache and reported SURVIVED on code the guard never saw. A mutation harness that runs Python now deletes the bytecode and runs with `PYTHONDONTWRITEBYTECODE=1` (`.freebuff/mutate_page_cost.py` says so at the top, including the two anchors that had matched an earlier occurrence: `query_meter.trip()` followed by `return self._query.execute()` appears in both the read and the write path). A run that stops reporting cost is **exit 2, never a pass** — a baseline that knows what a page costs while the measurement does not would silently retire the payload and query axes. Guards: `tests/unit/test_db_roundtrip_meter.py` (the header on every response including `0`, per-attempt counting, the thread boundary asserted rather than assumed, the cache replay, and the harness's own aggregation — a page that did not answer `200` and a header that is missing both read as *not measured*, never as zero) plus three new classes in `tests/unit/test_perf_gate.py` — mutation-checked, **18/18 injected defects caught**. Measured on the local app against the live project: `/student/settings` 57.6 KB / 1 query (the heaviest page), `/student/dashboard` 43.2 KB / 5 queries after the cache fix, `/student/exams` 39.6 KB / 2, `/teacher/dashboard` 54.6 KB / 6.
- **Writing that conf file found the claims gate switched off.** The deploy sources `/etc/scangrade-claims.conf` and passes every `CLAIMS_*` setting as an environment variable — and `claims_gate.py` read only `argv`, so `CLAIMS_BASE_URL` arrived, was ignored, and **every production run ended at `no --base URL`, exit 2** on a gate that looked installed and healthy (which is why `/var/lib/scangrade-deploy/claims` had never been created, and why a reassuring "could not measure" one-liner was the only trace). Both gates read their conf through `env_default()` now, and `tests/unit/test_perf_gate.py` fails if the installer writes a setting its gate never looks at, or if the deploy stops passing one
- **A `select()` list and the code below it are one contract, and a cached page needs every writer to drop it.** The teacher dashboard's "Answer key not set yet" card was computed from `exam.get("answer_key")` on a row the route no longer asked for: `fb9aef2` trimmed that `select("*")` to an explicit column list and left `answer_key` out, and a column absent from the list is `None`, which reads as "no key" — so **every** exam with an MCQ question was flagged **permanently**, and nothing a teacher could do cleared it (their keys were all in the database: one as a JSON object, one as the JSON *string* the answer-key page writes). Live reproduction against the teacher who reported it: the shipped code flags **2** exams (`['Mid Semester 1', 'Physics Unit Test']`) and the fixed code flags **0**. Half of what was reported was a second defect — the dashboard caches for 20 s and only `publish` ever dropped it, so even a true judgement looked stuck; the writers now call one helper, `_invalidate_teacher_dashboard()`. The judgement is one named function, `_needs_answer_key()`, with `_as_dict()` for the jsonb-returned-as-string form, and it uses the test the scoring path makes (`student.py` builds `mcq_count` from the *key*, so no key means the MCQ half is 0). A **partially** filled key is deliberately not flagged: it does not produce 0, it inflates the score — a different defect, and flagging it here would make the card's own sentence false. Guard: `tests/unit/test_dashboard_answer_key.py` (truth table, the select columns, and every writer dropping the cache) — mutation-checked, 4/4 defects caught
- **The capacity page is the artifacts, and a new measurement is the whole update.** `https://scangrade.web.id/capacity` (`app/templates/public/capacity.html`, route in `public.py`, reader in `app/services/capacity_service.py`) renders `docs/measurements/` directly: one row per rung, one card per run (harness, date, sessions signed in, requests, duration, p50 range, worst p95, errors, 429s, 5xx, identities verified), each with a download link to the raw file, plus the deploy gates' own machine-local history from `/var/lib/scangrade-deploy/{claims,perf}/history.jsonl` when it is readable. **There is no number in the template**: drop a new artifact in the directory, commit it, and the page grows a row — that auto-update property has its own test. Where a rung has two independent measurements the page publishes the **worse** bound, never the better one. Dates are honest by policy: the artifact's `measured_at` if it carries one, else its **git commit date**, else *date not recorded* — never the mtime, because a fresh clone stamps every file with today (`loadtest_concurrent.py` writes `measured_at` now, so future artifacts carry their own). The per-rung figures are the same strings the landing page publishes, and `test_the_two_public_pages_agree_about_the_same_files` compares the landing block — parsed by `test_landing_claims.py`'s own reader, kept independent on purpose — against this service, so a reader bug on either side is a disagreement rather than two pages wrong together. The recommended limit, the configuration, and every empty state (no artifact, no gate record, an unreadable directory, malformed JSONL) are covered. The page is bilingual EN/ID and carries **its own language button**, because the anonymous path renders no navbar — a guard fails if that toggle disappears, since bilingual copy nobody can switch is copy for one reader. `/capacity/evidence/<name>` serves only names the report built, as `text/plain`; it has **two locks** (the whitelist and a resolve check) and the mutation run showed the resolve check is the load-bearing one — through the URL a hostile name mostly dies at Werkzeug, so the guard also tests `artifact_path()` directly. A hardcoded figure cannot ship: the guard asserts each rung's p50/p95/error text equals what its artifacts measured. Guard: `tests/unit/test_capacity_page.py` (30 tests); mutation-checked, 6 of 7 injected defects caught — the miss being the whitelist alone, which the resolve check still covers

- **Measured capacity of the production VPS** (measured 14 Sep 2026 — do not advertise more without re-measuring): the box is **1 vCPU / 957 MB** (3 gevent workers). Ceiling is **~20 requests/s**, and it is **CPU-bound page rendering** (`vmstat`: user 90–93%, idle 0%, `wa` ≈0 — not network, memory, or Supabase). Concurrency curve, one account per session, identity verified: **50 murid** → p50 82–620 ms, p95 ≤ 1.7 s, 0% error · **100** → p50 1.9 s · **150** → p50 4.6–5.9 s · **500 sessions** → only 174 got past login (nginx per-IP login burst), p50 6.3–7.0 s, 2.3% 429, **zero 5xx**. Server-side `localhost /health` went 0.006 s idle → 5.03 s under load. Harness: `loadtest_concurrent.py --duration N` (endurance mode); guard: `tests/unit/test_landing_claims.py`
- **`locustfile.py` now authenticates for real** (fixed 14 Sep 2026) — previously it posted its login with no CSRF token (every attempt 403), judged success by HTTP status although a *rejected* login also answers 200, and drew from `siswa1..siswa1000_smp` of which six exist; every figure attributed to it was a logged-out visitor following redirects. It now reads the CSRF token off the login page, judges success by **where the response landed** (`/dashboard`), records a login that does not land there as a Locust **failure**, takes a **distinct roster account per virtual user** (`load_roster` shared with `loadtest_concurrent.py`) and **refuses to run** when `--users` exceeds the roster, verifies `/auth/me` identity per session, declares `wait_time = between(1, 3)` (without it Locust's `constant(0)` fires ~180 req/s at one account and the rate limiter's 429s look like a server failure), counts status codes for **every** request via `_RecordingSession`, and writes its JSON artifact **before** printing. Guarded by `tests/unit/test_locust_harness.py`, which drives the real functions in a child process (importing Locust calls `gevent.monkey.patch_all()`)
- **Claims gate** (`deploy/claims_gate.py`, deploy Gate 5, after the reload): re-measures **the rung the landing page itself advertises** — parsed out of `app/templates/landing.html`, not from a constant — and refuses the release when the box no longer behaves the way the page says. Compares the **worst signed-in page endpoint** (never a login burst: that is a different claim, and folding it in made a healthy app fail) against the published p50/p95, with `LATENCY_SLACK=2.0` and `ERROR_SLACK_PCT=1.0`. Two strikes: one divergent probe is contention, two are a stale claim. exit 0 pass · 1 confirmed divergence (rolls back only if `CLAIMS_ENFORCE="true"`) · 2 could not measure (no roster, box already busy, unconfirmed) and **never** a rollback. Needs one account per session (`.freebuff/lt_roster.json`, gitignored → survives a rollback reset). Evidence: `/var/lib/scangrade-deploy/claims/history.jsonl`. Guard: `tests/unit/test_claims_gate.py`
- **Landing-page rows are recomputed from `docs/measurements/`** (14 Sep 2026: 50/100/150 are 60-second endurance runs; the 500\* row is the recorded 10-minute run). Published: **50** → p50 64–962 ms, worst p95 3.7 s, 0% · **100** → 1.4–2.2 s, ≤ 4.2 s, 0% · **150** → 3.7–4.4 s, ≤ 15.2 s, 0% · **500\*** → 5.6–7.0 s, ≤ 10.8 s, 2.31%. A rung may have **several** artifacts (`rung-050.json` and `locust-050.json` are two independent measurements of the same load) and the published bound is the **worst** figure any of them produced — never the better one, because `claims_gate.py` holds the page to 2× of what it advertises and this box has really been observed at p50 1464 ms on the 50-student rung. Every artifact behind a row must also prove it authenticated (`logins_failed == 0`, identities verified), so a harness that cannot log in can never back a number again. The old 50-row (p50 82–620 ms) was replaced because a probe did not reproduce it. **Shape matters**: a 12 s probe measures 1464 ms at the same rung, because 50 simultaneous logins dominate the opening seconds — the published rows are 60 s for that reason, and the gate probes for 30 s. `tests/unit/test_landing_claims.py` recomputes every row from the artifacts, so the page and its evidence cannot drift
- **The Supabase client is one instance per app** (`current_app.extensions["supabase"]`), shared by every request. That is safe under the **gevent** workers production uses (no interleaving inside a worker), but under **thread**-level concurrency it is not: the Flask dev server at 50 concurrent sessions produced **2.1% 500s** (`RemoteProtocolError: Server disconnected` on `profiles`/`submissions` reads, 12–14 per run, reproducible in both probes), while production measured **0 × 5xx** on the identical probe. Do not move to gunicorn `gthread` (or any threaded worker) without making the client thread-local first
- **Measured capacity of the production VPS** (measured 14 Sep 2026 — do not advertise more without re-measuring): the box is **1 vCPU / 957 MB** (3 gevent workers). Ceiling is **~20 requests/s**, and it is **CPU-bound page rendering** (`vmstat`: user 90–93%, idle 0%, `wa` ≈0 — not network, memory, or Supabase). Concurrency curve, one account per session, identity verified: **50 murid** → p50 82–620 ms, p95 ≤ 1.7 s, 0% error · **100** → p50 1.9 s · **150** → p50 4.6–5.9 s · **500 sessions** → only 174 got past login (nginx per-IP login burst), p50 6.3–7.0 s, 2.3% 429, **zero 5xx**. Server-side `localhost /health` went 0.006 s idle → 5.03 s under load. Harness: `loadtest_concurrent.py --duration N` (endurance mode); guard: `tests/unit/test_landing_claims.py`
- **`locustfile.py` now authenticates for real** (fixed 14 Sep 2026) — previously it posted its login with no CSRF token (every attempt 403), judged success by HTTP status although a *rejected* login also answers 200, and drew from `siswa1..siswa1000_smp` of which six exist; every figure attributed to it was a logged-out visitor following redirects. It now reads the CSRF token off the login page, judges success by **where the response landed** (`/dashboard`), records a login that does not land there as a Locust **failure**, takes a **distinct roster account per virtual user** (`load_roster` shared with `loadtest_concurrent.py`) and **refuses to run** when `--users` exceeds the roster, verifies `/auth/me` identity per session, declares `wait_time = between(1, 3)` (without it Locust's `constant(0)` fires ~180 req/s at one account and the rate limiter's 429s look like a server failure), counts status codes for **every** request via `_RecordingSession`, and writes its JSON artifact **before** printing. Guarded by `tests/unit/test_locust_harness.py`, which drives the real functions in a child process (importing Locust calls `gevent.monkey.patch_all()`)
- **Claims gate** (`deploy/claims_gate.py`, deploy Gate 5, after the reload): re-measures **the rung the landing page itself advertises** — parsed out of `app/templates/landing.html`, not from a constant — and refuses the release when the box no longer behaves the way the page says. Compares the **worst signed-in page endpoint** (never a login burst: that is a different claim, and folding it in made a healthy app fail) against the published p50/p95, with `LATENCY_SLACK=2.0` and `ERROR_SLACK_PCT=1.0`. Two strikes: one divergent probe is contention, two are a stale claim. exit 0 pass · 1 confirmed divergence (rolls back only if `CLAIMS_ENFORCE="true"`) · 2 could not measure (no roster, box already busy, unconfirmed) and **never** a rollback. Needs one account per session (`.freebuff/lt_roster.json`, gitignored → survives a rollback reset). Evidence: `/var/lib/scangrade-deploy/claims/history.jsonl`. Guard: `tests/unit/test_claims_gate.py`
- **Landing-page rows are recomputed from `docs/measurements/`** (14 Sep 2026: 50/100/150 are 60-second endurance runs; the 500\* row is the recorded 10-minute run). Published: **50** → p50 64–962 ms, worst p95 3.7 s, 0% · **100** → 1.4–2.2 s, ≤ 4.2 s, 0% · **150** → 3.7–4.4 s, ≤ 15.2 s, 0% · **500\*** → 5.6–7.0 s, ≤ 10.8 s, 2.31%. A rung may have **several** artifacts (`rung-050.json` and `locust-050.json` are two independent measurements of the same load) and the published bound is the **worst** figure any of them produced — never the better one, because `claims_gate.py` holds the page to 2× of what it advertises and this box has really been observed at p50 1464 ms on the 50-student rung. Every artifact behind a row must also prove it authenticated (`logins_failed == 0`, identities verified), so a harness that cannot log in can never back a number again. The old 50-row (p50 82–620 ms) was replaced because a probe did not reproduce it. **Shape matters**: a 12 s probe measures 1464 ms at the same rung, because 50 simultaneous logins dominate the opening seconds — the published rows are 60 s for that reason, and the gate probes for 30 s. `tests/unit/test_landing_claims.py` recomputes every row from the artifacts, so the page and its evidence cannot drift Evidence files have to be ones git carries: `.gitignore` excludes `*.log`, so the 500\* run and the Locust console output are `.txt`, and `tests/unit/test_landing_claims.py` fails when any file in `docs/measurements/` is one no fresh clone would receive.
- **The Supabase client is one instance per app** (`current_app.extensions["supabase"]`), shared by every request. That is safe under the **gevent** workers production uses (no interleaving inside a worker), but under **thread**-level concurrency it is not: the Flask dev server at 50 concurrent sessions produced **2.1% 500s** (`RemoteProtocolError: Server disconnected` on `profiles`/`submissions` reads, 12–14 per run, reproducible in both probes), while production measured **0 × 5xx** on the identical probe. Do not move to gunicorn `gthread` (or any threaded worker) without making the client thread-local first
- **The exam page has two ways around the paper, and the paper sits *beside* the column.** `student/take_exam.html` offers `classic` (the question list on the left, each question with its own page strip) and `rail` (the pages as thumbnails, in order, which is what a question spread over four A4 pages needs). Both are remembered per device in `localStorage` (`sg_exam_layout`, `sg_exam_rail_mode`, `sg_exam_rail_hidden`, `sg_exam_nav_hidden`), the column collapses to a strip that brings it back, and in rail mode the same column switches between the pages and the question list so neither is ever out of reach — hiding the rail must not hide the way to answer a question. Picking a page follows the paper: `goToPage()` moves `currentQ` to the question that owns the page, because "page 7" is what the student is thinking, not "question 3's page 1". The rail shows `thumb_NNN.png` next to each `page_NNN.png` (180 px wide, **11.8×** lighter on a measured 12-page exam — 21 KB vs 242 KB; pointing the rail at the 150-dpi pages would make a student download the whole exam again), generated at upload by `pdf_service.ensure_page_thumbs()` and backfilled by the exam route, which is idempotent and free once the files exist. `@error` falls back to the full page so an exam uploaded before thumbnails existed still renders. Three defects were fixed here, all silent: a stray `</div>` closed the layout row *before* the main column, so `flex-1` did nothing and the paper was laid out **under** the sidebar rather than beside it; `chromeHidden` was a **method** used bare in `x-show`, and a bare method name is a truthy function object, so `!chromeHidden` read false for ever and the column was created, styled and never shown; and the question watcher jumped to a question's range start on *every* switch, throwing away a page the student had chosen. Guards: `tests/unit/test_exam_page_rail.py` (16 tests — thumbnails not pages, the fallback, the owner follow, the in-range rule, the remembered layout, both languages, tag balance, and a generic rule that every binding names a value, not an uncalled method). Mutation-checked, 10/10 injected defects caught.
- **The controls that answer the paper can sit under it or beside it, and a phone has to be told no by the *stylesheet*, not only by the button.** ZipGrade draws the A B C D E bubbles in a column on the **left** of the sheet, and that is how a paper answer sheet is read — sheet on the right, options beside it, both in view so marking an answer is not a scroll away from the question. `student/take_exam.html` now offers that as a second setting (`sg_exam_answers_side`, remembered per device) beside the layout one, and it applies to **all five kinds**: a choice becomes a stacked strip, true/false one column, matching and drag & drop a `20rem` board, an essay `45%` — each capped so the paper stays the majority of the row. Two things there are measurement, not taste. First, `.exam-stage` (one wrapper around the paper *and* the answer card) is what makes it a layout rather than a second copy of every control, so no kind can be left behind on the wrong side. Second — and this is the defect to remember — the first version scoped only the **button** to 641px and left the layout rules at the top level, so a phone with `left` stored obeyed it: measured at 375px the paper was **179px** wide with the answer strip holding 152 of the 375, the identical failure this page already fixed once for the question column. The rules are inside the query now and `tests/unit/test_exam_page_rail.py` fails if any side-layout selector appears outside it — scanned over the *whole* stylesheet, not just the `@media` blocks, because a top-level rule belongs to no block and a block-only scan cannot see it (the first version of that guard had exactly that hole, found by mutation). Also measured: the option row needs `width: 100%` (`flex items-center flex-wrap` made it only as wide as its widest button — 54px in a 152px column — so `stretch` had nothing to stretch to), and the card is `position: sticky; top: 84px` because a paper can be four times its height and a column that scrolls away is the scrolling this layout exists to remove (`84` = the exam bar's measured 70–74px single row plus its gap). **`left` is the default** — it is the arrangement a paper answer sheet is read in — and nothing in the setting refuses it on a small screen, so the phone's safety comes entirely from the `min-width: 641px` query; the default is executed in node from the template's own expression (nothing stored, a stored `below`, a stored `left`, a value no branch recognises, and a `localStorage` that throws) rather than described.
- **An exam sitting has one door, and the options column scrolls itself.** Two halves of one report ("the ScanGrade sidebar is not hidden during the assessment"; "the options should be on the left of the canvas and scroll up and down"). **The chrome goes for the sitting.** `base.html` names its three chrome regions — `sg-app-sidebar`, `sg-app-topbar`, `sg-app-bottomnav` — and offers a `{% block body_class %}`; `student/take_exam.html` asks for `sg-exam` and hides all three plus the sidebar's teleported scrim. On a 1280px window that is **256px of width the paper and the options were not getting**: measured, the paper at 1280 went from 650px to **906px** — the sidebar's own width, exactly — and `main` now reads 1280/1024/768/375 with **zero** horizontal overflow at every one of them. Hiding the chrome is also the honest reading of this page's own anti-cheat, which *counts* leaving, so the app should not offer the door it is counting — and it is what makes a mis-tap on "My Exams" mid-question impossible. The hooks are a contract rather than a DOM shape for a reason worth remembering: a rule written `body > div > aside` is turned off silently by one extra wrapper, and "the sidebar is hidden during an exam" is not a thing a test can see, so the guard asserts the two files agree on the names. What the student keeps is everything a sitting needs, and it is all in the exam's own bar (title, code, sync, timer, penalty, column controls, Submit) plus the page's own three language controls — the app draws those three times precisely because it knows its chrome may not be reachable. What it loses with the chrome is the theme toggle and the account menu; the stored theme still applies. The phone's calculator dropped its `bottom: 72px`, which was clearing a bottom nav that no longer exists. **The column scrolls itself.** A ten-pair matching board measures **1,358px of content** at 768 and 763px at 1280 — taller than the window — and a column that is merely sticky does not let you reach the bottom of it without scrolling the question out of view, which is the scrolling this layout exists to remove. The card now keeps its own box (`overflow-y: auto`) inside `calc(100vh - N)`. **`N` was wrong the first time, systematically, and only measurement showed it:** sized against the card's *stuck* offset (84px), the ceiling put the column's bottom **6px under the fold at the page's opening position on 1280, 1024 and 1920 alike** — because the card's natural top is not 84px but **110px** (the page's own 24px of padding and the exam bar's 12px gap counted a second time), and 98px at 768. `calc(100vh - 120px)` keeps 10px of clearance at the largest flow offset and still leaves 36px below the box once the card is stuck; the guard holds the rule to both figures, and to the numbers its own comment records, so a stale comment cannot be reasoned from. Guards: four classes in `tests/unit/test_exam_page_rail.py` (44→45 tests) — the page asks and `base.html` answers, all three hooks plus the scrim are hidden at every width (a rule that reappears inside a media query would leave the chrome on at every other width), the box exists with a ceiling that clears both where the card sticks and where it starts, and the default/setter pair is executed rather than read. Mutation-checked, **19/19 injected defects caught** — including the historical `104px` ceiling, the ceiling derived from the stuck offset alone, a renamed hook in `base.html` only, and a `body_class` block that is present but not on the `<body>` tag.
- **The touch floor is a question about the finger, not the width — and it was asked wrongly.** Every mobile rule on `take_exam.html` lived under `@media (max-width: 640px)`, which is a *proxy* for "a finger is tapping". Measured at **768 px** (a tablet, above the breakpoint, so none of those rules applied) the calculator's keys were **16 px tall with an 8 px-wide close button** and the drawing tools **25 px** — the same defect the phone rules were written to fix, on a bigger screen. The floor is now also asked as `@media (pointer: coarse) and (min-width: 641px)`, so a mouse desktop keeps its compact panel and any touch screen gets targets a finger can hit; the drawing row becomes a **single swipeable row** there too, because at 44 px the tools wrapped to **three rows / 157 px** (a fifth of the screen) where at 25 px they fitted in two — raising a target is not a reason to charge the paper more room. Two more silent defects were fixed in the same pass and both are *measured*, not read: the draw toolbar's icon-only tools had `min-height` but no `min-width` (**37 px**, and 44×37 is still a miss), and the calculator and drawing tools open only when `!showExamAgreement`, so an audit that skips the modal reads a perfectly good panel as `display:none`. Verified by driving headless Chrome over CDP at a real 320/375/768 px viewport with the app's own session cookie: OMR bubbles one line at 44×44 with zero page overflow, the essay canvas reached at its centre by `elementFromPoint` ("the canvas", so no overlay shadows the pen), keys 44×44 and **7+3 actually computing 10**. Guard: three tests in `tests/unit/test_exam_page_rail.py` (the coarse-pointer block exists and covers both axes on calculator, tools and bubbles; the touch row stays `nowrap` + `overflow-x: auto`; and the phone block and the touch block cannot disagree about the floor) — mutation-checked, **8/8 injected defects caught**, including deleting the block and answering the width instead of the pointer. **Do not measure this page by accepting its terms**: `agreed = true` starts the attempt, arms the anti-cheat and, under headless where fullscreen is impossible, auto-submits a real student's sitting — it did, and the row had to be restored to `draft`.
- **The teacher's page field is kept, because it drives the student's paper — but it is no longer typed, and no longer required.** Assessment of `exam_form.html`'s per-question "PDF page" input: an empty page is harmless (the student page simply does not turn the paper, and the rail shows no owner badge), yet the form demanded one (`required`) with no label and only a placeholder — so a teacher who did not know the number had to invent it, and an invented page sends the student to the wrong part of the paper, which is worse than an empty one. It is now optional, labelled, `id`s match their `for`, and the parse prefills it (`copy.pages = q.pages \|\| String(q.page)`), with a "filled automatically" badge that clears on the first keystroke (`_pageAuto`). Derivation lives in `pdf_parser.question_pages()` (per-page markdown, whitespace-insensitive, first match wins, never a guess) and is covered by `tests/unit/test_question_pages.py`.
- **`_normalise_exam_json()` exists because `question_pages` is a JSON *string* in a jsonb column.** `question_types` beside it is a real object, but the page column is written with `json.dumps`, so the builder's `question_pages[i]` indexed a string by `'0'`..`'4'` and returned **characters**: the exam-edit form showed `{`, `"`, `0`, `"`, `:` in the page fields — five pieces of nonsense a teacher could not recognise as wrong — and saving wrote them back over the real mapping, after which `parsePageRange("{")` finds no pages and the student's paper silently stops turning. The GET branch of `/teacher/exams/<id>` parses every jsonb field the builder reads before rendering; the list is asserted against the template so a field added later cannot be forgotten. Guard: `tests/unit/test_exam_form_json_fields.py`
- **The answer sheet is read by asking "is there a 6 mm square of ink centred here?", not by segmenting the photo and hoping the blob survives.** `omr_service.py`'s contour detector was replaced by a matched filter — the mean of a square window minus the mean of a square ring around it, over a ladder of window sizes — because measurement retired the contour version: on a hard simulated phone photo all four corner markers *were* found, within 1.8 px of their true centres, but at ranks **0, 10, 64 and 226** with measured sides of **74, 41, 33 and 47 px against a real 47**. Blur, JPEG and the threshold between them merged a marker with the ink beside it in one place and eroded it to two-thirds of its size in another, so the two properties the gates leaned on hardest — solidity and size — were exactly the two that fell apart. A matched filter has no such failure mode: the response peaks at the centre whatever the edges did, and the size falls out of *which window* peaked (response at the four true centres: 210, 202, 201, 193 of 255, against 158 for the next-best anything on the sheet).
- **`RETR_EXTERNAL` returned zero marker candidates on every photographed sheet.** Every dark mark is inside the page, and a photographed page has a bright closed frame where paper meets desk, so the frame was the outermost contour and every mark on the sheet was one of its holes — never returned. The flat scan the detector was calibrated against has no desk in it, which is why the bug was invisible. `RETR_LIST` took the same code from **0 candidates to 617**. (Kept as a rule because it is the failure mode of any contour search over a photo: *when the page is an object lying on something, the outermost contour is not the page.*)
- **A discarded binary: `Otsu` over an illumination-divided copy, with the blur σ = max/6.** That σ estimates the lighting over a window nearly as wide as the page, so the estimate barely varied where the lighting did, and the clip to 190 pulled paper and desk to the same value — the result's histogram had paper in a broad band around 170 and the desk right beside it, Otsu split *the band* rather than the marks, and `_square_candidates` saw nothing at all. The blur is now computed on a 1/8 copy (a Gaussian that wide costs seconds at full resolution), and the marker search does not depend on a global threshold at all.
- **Marker selection is anchored on the convex hull of marker-*plausible* candidates, and "plausible" is load-bearing.** Ranking candidates by how dark they look and taking the top 14 put the bottom-left marker at rank **34 of 61** under an uneven light — a gradient across the page lowers a marker's absolute contrast below that of the filled bubbles in the bright half — so the true four were never considered together and the search returned a rectangle of four filled bubbles, 1344 px from the page. The four markers are at the page's own corners, so they are the extreme points by construction; but the hull must be taken over blobs that already pass the quiet-zone test, because a sheet on a *darker* surface produces a bright closed edge that is a row of candidates **outboard of the bottom markers** — measured on a flat scan on a grey desk, the hull of all 45 candidates had 7 vertices, three of them desk edge, and the two bottom markers were **inside** it.
- **Refusal is by what the read found, not by how tilted the page looks.** The gate was `aniso > 1.45`, and it was retired for two measured reasons. It rejected photographs whose markers were correct to **1.7 px** and whose answers came back **44/44 with 0 wrong**, because a 55%-darker corner and real perspective push a legitimate page to 1.46. And it had no power against the thing it was aimed at: four filled bubbles mistaken for markers also measure **1.23–1.47**, *below* the very line that was rejecting good sheets. `grid_match` — the fraction of bubbles whose **printed outline** is where the model says it is — replaces it, and it is the only test here that checks the whole sheet rather than four points on it. Calibrated from both ends over 15 correct warps and the 45 wrong ones that go with them: the gate `OUTLINE_PRESENT=10.0` is the peak of the margin curve (worst correct warp **0.556** vs best wrong warp **0.140**, 4.0×), and `GRID_MATCH_MIN=0.28` is the geometric midpoint. **A blank sheet measures 1.0, not 0** — an unanswered bubble still has its printed outline — which is what lets an empty answer set be told from a failed read, and it is why the pipeline refuses a wrong warp instead of returning a confident-looking grid.
- **The entry path that used to be a silent fallback is now guarded.** A new-format sheet whose markers were missed used to be handed to the **legacy** reader, which maps corners by its own constants and would have read the wrong part of the page with no error. `sheet_code_present()` looks for one of this app's layout codes anywhere in the frame — the old L-rule paper has no QR at all — and a photograph that carries a code but has no findable markers is *refused* with an actionable message instead of being read with the other geometry.
- **Measured, on simulated phone photos only** (perspective 6–24% of the frame, rotation 3–16°, blur σ0.8–2.6, a corner up to 70% darker, JPEG 40–100, page filling 3–45% margins): **394/394** answers recovered exactly on the harness's three conditions × 9 seeds, and **1456/1456** across a wider 9-condition × 4-seed matrix, with **zero wrong answers and zero invented ones in every one of the 36 cases** — the failures are refusals, never misreads. Marker detection within 25 px on 9/9 and 33/36; the four remaining cases are refusals. **Real paper photographs have not been measured yet** — that is the next honest step, and the numbers above are a simulator's, not a school's. Guard: `tests/unit/test_omr_sheet_scan.py` (the printer generates, the simulated phone photographs, the reader must return exactly what was filled; a wrong rotation is refused; a blank sheet is not; a photograph missing a corner is; the markers land within 25 px of their true centres; and the four defects above each pinned by the property that let it happen). **To measure real paper — the one thing a simulator cannot stand in for — run `test_scripts/check_omr_photo.py photo.jpg --expect A,B,C,...`**: it prints the sheet code read from the printed QR, markers found, `grid_match`, the page covered, the NISN, every answer with its confidence, and writes the reader's own annotated view next to the photograph so a failure can be looked at rather than guessed. Exit 0 read / 1 refused / 2 undecodable, so it scripts.
- **The generator's preview is documentation, and it had drifted from the sheet it describes.** It drew the four corner marks as **L-shaped rules** — the geometry the sheet used *before* `omr_layout` existed, which the scanner can no longer read, because a rule is two thin strokes whose apparent thickness changes with distance, blur and JPEG, while a filled square is a region a hand-held photo still contains. A teacher checking the preview was shown a sheet their own scanner could not register, and the preview drew no student-ID band at all even though the scanner reads the ID off the same photograph. Three tests hold the preview to the format (no corner rule, exactly four solid marks, the ID band present).

- **A photograph is one page, and the sheet's grid is not unique across pages.** `omr_layout` caps the grid at 4 columns × 20 rows = **80 questions per page**, so any exam over 80 questions prints as two pages — and `read_answers` looped `for page in range(layout.pages)` over a *single* warped image. It did not read more of the sheet: page 2's question 81 is printed at the same millimetres as page 1's question 1, so `option_centre_mm(q, o, page)` mapped the second pass onto the same bubbles and reported them under a second question number. Measured on the committed `docs/ljk/LJK-uas-100-soal.pdf`: photographing page 1 alone returned answers for **all 100** questions, 81–100 taken from 1–20's marks, with nothing in the result saying so — twenty scores computed from the wrong part of the paper. The page is now *passed in* (`read_answers(..., page_index)`, `process_scan(..., page_index)`, `draw_debug_image(..., page_index)`) and the result reports `page_index` / `page_count` / `first_question` / `last_question`. All three doors carry it: the sync route and the Celery task read a `page` form field, the teacher's scan screen asks which page (only when the sheet has more than one, and it mirrors the 80-per-page grid), and `scan_save` **merges** the page it saw instead of replacing the answer set, grading over the union — a scan of a page never saved goes through even on a graded row, a re-scan of a page already saved is still refused. Note *why* this stayed invisible: 160 questions is two pages of 80 indistinguishable by ink density, so the wrong page returns a complete, confident answer set; at 100 questions the second page is sparse and the `grid_match` self-check happens to refuse it, which is luck, not coverage. `docs/ljk/` holds the printable formats (20/40/50/100/160 questions, 4- and 5-option) with a printing guide, and they are also the test fixture. Guards: `tests/unit/test_omr_sheet_scan.py` — `TestOnePhotographIsOnePage` (a page returns only its own questions with 0 wrong/missed/invented; the wrong-page read demonstrated to be plausible rather than refused; the sparse-page contrast; page clamping; a single-page sheet unchanged), `TestTheCommittedLJKFormatsScanBack` (every page of every committed PDF, filled and read back from the bytes on disk), `TestThePageTravelsWithTheScan` (the page reaches the reader from all three doors, and `scan_save` merges and re-grades) — mutation-checked, **12/12 injected defects caught**, and the first run taught the lesson that 4 of them had to be *tightened*: a substring guard satisfied by another occurrence of the same expression guards nothing

- **The grading page put a rail *beside* the answer on a 375 px screen, and no width guard could see it.** Measured in headless Chrome at a real viewport with the app's own session cookie (`/teacher/grade/<id>`, teacher *Eko Fisika*, 5 questions): the row was a bare `flex`, the question rail was `shrink-0` at its natural width, so of the **343** px available the rail took **126** and the answer pane got **205** — the card the teacher marks **171**, each of the two answer cells **79**, and a student's handwritten canvas the same 171. Nothing overflowed, so the existing responsive checks were silent: the page *fit*, it was just a desktop two-column layout squeezed onto a phone. The rail is now a **single swipeable strip above the answer** (`flex flex-col lg:flex-row` on the row, `flex items-center gap-2 overflow-x-auto lg:block lg:overflow-visible` on the card, `w-full lg:w-auto`, the number row `flex-row lg:flex-col shrink-0`), measured after at 320/375/414/768: pane 288/343/382/736, card 254/309/348/448, cells 121/149/168/218, page overflow **0** everywhere; at 1024/1280 it is byte-for-byte the desktop it was (rail **126x370** sticky, pane 582/838, card 448, cells 218). With 40 questions in the strip it stays **66 px** tall, scrolls 1383 px of content sideways, and still adds nothing to the page's width. Three smaller things in the same pass: the rail's `style="max-height:calc(100vh - 170px)"` moved into CSS behind a breakpoint (*an inline style cannot be answered per width*, and the cap had no `overflow-y` with it, so a 40-question rail painted its buttons past the card's own background instead of scrolling); `.sticky-questions` is `position: static` below `lg` (the header card above is already sticky and wraps to ~174 px at 375 px, so a second sticky bar left about half of a 900 px screen for the essay being marked); and `lg` is the breakpoint because it is where this page's sidebar becomes a sidebar. Guard: `TestATwoColumnWorkAreaCollapsesOnAPhone` in `tests/unit/test_mobile_layout.py` (the row stacks, the rail takes the phone's width, the strip equals one scrolling row rather than a wrapping block, the desktop stays a column, the rail is not sticky below `lg`, and the height cap is in CSS with its scroll) — mutation-checked, **9/9 injected defects caught**. Deliberately *not* a repo-wide ban on inline `max-height`: four other templates cap a height on purpose (`base.html`'s sidebar at `calc(100vh - 8rem)`, which a phone never shows; the two monitor boxes, which set `overflow-y: auto` themselves). Measured but left alone: at 320 px the global 7-item bottom nav gives three items 28–41 px of width (54 px tall, adjacent, no dead space) — raising them to 44 px would cost the bar a swipe on the narrowest phones, and that trade belongs to the chrome, not to this page. **On a phone the sticky chrome is the other quarter of the screen**: at 320 px the topbar (64) plus the header card (198, `sticky`) plus the bottom nav (55) hold **317 px = 35%** of a 900 px viewport, leaving 65% for the essay; at 375 it is 33%, at 768 (where the header card collapses to one row, 94 px) 24%. The rail is no longer one of them — `.sticky-questions` is `position: static` below `lg` — but the header card's own stickiness is a deliberate affordance (its *Simpan* button has to be reachable down a long essay), so changing it is a product decision, not a defect. The guard change that came with the chrome: `test_base_html_chrome_is_marked_region_by_region` used to name **two** bottom navs (`lg:hidden fixed bottom-0` and `fixed bottom-0`); the role-specific duplicate was removed, so it names one and a new `test_base_html_has_one_bottom_nav` keeps a twin from coming back — and it reads the four role branches *inside* the bar, because the sidebar branches on the same four names and a file-wide substring was satisfied by the sidebar's copy (that mutation was MISSED until the check was scoped; 4/4 after)

- **A stylesheet is committed here, so a template can outgrow it in silence — and the first thing this check caught was itself.** `tailwind.css` is built by hand (`npm run css:build`) and served as a cached static file, so a template that gains a utility plus a build nobody ran ships a class that does not exist: no error, no missing resource, and the element keeps whatever it inherited — which is how two of the three "Log in" buttons on `/demo` shipped as white text on a white box. `tests/unit/test_tailwind_class_names.py` catches that for the *colour* utilities a binding names; `deploy/css_freshness.py` is the general answer: it rebuilds with the project's own `css:build` command (parsed out of `package.json`, so a changed flag cannot make the check measure a different stylesheet) and compares the result, naming the template that asked for each missing class — `.rotate-45 (app/templates/teacher/settings.html)` — with `npm run css:build` as the fix. Exit codes follow the other gates: 0 in sync · 1 stale · **2 could not measure** (no node, no `node_modules`, a build that failed or wrote nothing), and a check that cannot answer never rejects a release. Two details make it trustworthy, and both came out of measuring production rather than reasoning about it: it compares **what a browser applies**, comments stripped, because `--minify` keeps Tailwind's banner — `/*! tailwindcss v3.4.17 … */` on the laptop and `v3.4.19` on the box, each matching its own lockfile, producing the same **882** classes and byte-identical rules (102,660 bytes each after stripping) — so a raw byte comparison would have called production's perfectly good stylesheet stale and **refused every release from then on**; and the utility regex read `.4.17` straight out of that banner as a class name. Line endings are normalised as well (Git stores LF, checks out CRLF), so the laptop and the deploy agree. It runs inside `deploy/theme_gate.sh`, which the pre-commit hook *and* the auto-deploy already run, and the hook now also fires on `tailwind.config.js` / `package.json` changes, because those change what the build produces without touching a template. Guard: `tests/unit/test_css_freshness.py` (34 tests — the banner is not staleness, a changed *rule* still is, a plain check never writes the stylesheet it is checking, the command is the package's own, exit 2 never becomes exit 1, and the deploy's `-eq 2` branch carries no `reset --hard` while its failure branch still does) — mutation-checked, **13/13 injected defects caught**, after two guards had to be *tightened* first: an assertion over the whole theme-gate block still passed with a rollback added to the branch that must not have one, and the guard file's own path appears in the gate's docstring, so deleting that *mention* proved nothing about the `TESTS` line.

- **The API was handing out answer keys to anyone who asked, and only the database could say so.** `deploy/schema_contract.py` compares three things that must agree — the names the Python writes, the SQL this repository carries, and what PostgREST actually serves — and it began as a check of the first two only. The half that mattered was the third: pointing the **anon** key (the public one, in every page's HTML) at all 49 objects with no session, **8 answered with rows**. `exams` returned `{"0":"D","1":"B",…}` — the answer key of every published paper — because `exams_select_active` was `FOR SELECT USING (status = 'active' AND is_published = TRUE)`, and **a policy with no `TO` clause applies to `PUBLIC`, `anon` included**. `school_registration_requests` returned the `activation_code` of every pending school (what activates a new school admin), and `notifications` returned every staff message. The two views answered as well: a view without `security_invoker` runs as its owner and every policy underneath is skipped. `deploy/schema_contract.py --anon` is that measurement, and it is **read-only on purpose**: the catalogue of objects comes from the *service* key, because `/rest/v1/` asked with a public key answers 401 "Only the `service_role` API key can be used for this endpoint" (the first version read the description with the anon key, got zero definitions, and reported a confident `0 of 0 objects — OK`). Both public keys are probed — the legacy anon JWT and the newer publishable key — and both answered the same eight objects. The write half was taken by hand once and is **not** automated: `POST {}` with `Prefer: return=minimal` answered **400/23502** on `violation_logs`, `audit_logs`, `classes` and `schools` — a not-null violation, not the 401 a refused policy gives — so the insert had already passed row-level security and only the empty payload stopped it. That is worse than the read leak: a forged violation is the record the penalty is computed from, and `classes`/`schools` are tenant creation. Nothing was written (no row can satisfy `NOT NULL` with `{}`), and the probe stays read-only because on a table whose columns are all defaulted the same request would **create a row in production**. **311 of 320 policies in this repository have no `TO` clause**, which is fine where the expression asks for a session (`auth.uid() = id`) and a hole where it does not. Migration 028 drops the open ones, restates the `*_service` policies as `TO service_role` (the service key bypasses RLS, so they granted nothing the app needed and everything an attacker wanted), scopes the notification policies to the sender and the recipients, and turns `security_invoker` on. Nothing in the app breaks: `get_auth_client()` is called **only** from `auth.py` and `utils/auth.py` for GoTrue (`sign_up`, `sign_in_with_password`, `get_user`) and no client-side JS reads PostgREST at all. Rehearsed against production inside a transaction that was **rolled back** (PG 17.6) — 20 open policies before, none after, both views `security_invoker=true` — so the statements were exercised by the real database and nothing was committed.
- **The checker's first honest run was wrong by 72 names, and reading production found the drift in the other direction.** The contract read only `supabase/migrations/*.sql`, but `001_initial_schema.sql` is a two-line stub whose text is "Copy content from supabase/schema.sql", the base tables (`profiles`, `exams`, `submissions`, `classes`) live in `supabase/schema.sql`, and the AI and billing tables in `supabase/_COMPLETE_SETUP.sql` — so it reported **72 names the code uses that "no migration creates"**, every one of them a real column of a real table. A check that cries wolf on `submissions.score` is a check nobody reads. Two smaller defects in the same reader: a comma inside a JSON column default (`prompts JSONB DEFAULT '{"feedback": {…}}'`) split the blob into four invented "columns", and `ALTER TABLE … DROP COLUMN` was ignored, so migration 024's temporary `school_admin.school_id_new` looked like a column the database was missing. Then `--live` asked the API and found the opposite fact: **four columns and two views exist in production and in no file here** — `profiles.consent_at`, `profiles.pdp_agreed`, `school_settings.demo_settings` (read by `get_demo_settings()` on *every* page), `schools.email_domain`, `question_embeddings.context`, and the views `active_school_years` / `class_student_counts` — all added by hand in the SQL Editor, so a database rebuilt from this repository would not have them. Migration **027** declares them; `--live` reports an object the API serves that no file declares as a **failure** and a migration not yet pasted as *pending*, because those are opposite facts: one is a database ahead of the repository, the other the repository ahead of the database. One live defect came out of the code half: `profiles` has **no `email`** (addresses live in `auth.users`), and two helpers in `app/models/supabase_queries.py` selected `profiles!inner(id, full_name, email, …)` — refused with 42703, so any future caller gets an empty list. 
- **A role the database cannot hold is a branch that never runs.** The contract also reads the `CHECK (role IN (…))` constraint the repository's own SQL declares — **last one wins**, because `schema.sql` still says `student`/`teacher`/`admin` and migration 007 replaces it with `murid`/`guru`/`admin_sekolah`/`super_admin`, and a union would excuse a legacy name by finding it in the constraint it replaced — and then fails on any comparison against a name outside it. Measured against the live database: 43 `guru`, 523 `murid`, 7 `admin_sekolah`, 2 `super_admin`, and no legacy value at all. One site was found and it was a trap rather than a live bug: `whiteboard_service.list_whiteboards(role="teacher")` compared its parameter against `'teacher'`, so a caller passing `g.user_role` would have got `'guru' == 'teacher'` → false → the *student* branch. Both callers pass a literal, so nothing was broken; the parameter is a **view**, not a role, and is named `view` now.
- **The gate judges the state, not the history, or it can never pass again.** `DROP POLICY` and `CREATE POLICY` are applied in file order (and within a file by position, so the `DROP … IF EXISTS; CREATE …` pair every migration writes ends with the policy present), and base files come before numbered migrations — plain `sorted()` puts `007_multi_school…` before `schema.sql`, which would read the constraint it replaced as the one still in force. Same reason the schema reader is a union over all sources instead of last-writer-wins. Guard: `tests/unit/test_schema_contract.py` (**56 tests**) — the readers (a table constraint is not a column, a comma inside a JSON default is not a column boundary, a hyphenated table name, every `ADD COLUMN` in one `ALTER`, a temporary rename column dropped in the same migration, a view's explicit column list), the code reader (each column of a `select`, an embed's table *and* its columns, a finding that names the file and line rather than just the table, filters/order/writes), the policy state machine (open when there is no `TO` and no session predicate; closed by `TO authenticated` or by `auth.uid()`/`_is_role()`/`_user_school_id()`; closed by a later `DROP`; still open when the recreate is wide), the views, both directions of drift, the role vocabulary, five injected defects in a copy of the real tree — including **reproducing the 72-name original** by reading only the migrations — and the live probe's own plumbing (both public keys are read, nothing is public by design, an unanswerable probe returns `None` rather than a pass, the catalogue does not come from the public key, and the probe contains no `POST`). One test runs the tool with `PYTHONIOENCODING=ascii`, because an em dash in a *printed* string is a `UnicodeEncodeError` on a cp1252 box, and the gate would read that as a finding and refuse the release. It runs inside `deploy/theme_gate.sh` (exit 1 refuses, exit 2 says so without refusing), and the pre-commit hook now also fires on `app/**/*.py` and `supabase/**/*.sql`.
- **Not applied to production yet.** `python deploy/schema_contract.py --live` reports 16 declared-but-missing objects: migrations **021, 024, 026, 027 and 028** are written and rehearsed but have not been pasted into the SQL Editor, so the eight objects above are still open to the anonymous key in production. That is the migration queue, not a defect in the repository.

## Pertukaran Pesan — RBAC Matrix

| Fitur | super_admin | admin_sekolah | guru | murid |
|-------|-------------|---------------|------|-------|
| **Kirim Pengumuman** (broadcast) | ✅ All roles | ✅ Guru & Murid di sekolahnya | ✅ Hanya Murid | ❌ Dilarang |
| **Lihat Pengumuman** | ✅ Semua | ✅ Semua | ✅ Semua | ✅ Semua |
| **Kirim Pesan 1-on-1** (Percakapan) | ✅ Siapa saja | ✅ Guru/Murid di sekolahnya | ✅ Murid | ✅ Hanya Guru/Admin (diverifikasi role) |
| **Lihat Percakapan** | ✅ Semua partisipasi | ✅ Semua partisipasi | ✅ Semua partisipasi | ✅ Hanya dgn Guru/Admin (filter server-side) |
| **Edit/Hapus Broadcast** | ✅ Semua | ✅ Milik sendiri | ✅ Milik sendiri | ❌ |
| **Setujui Hapus Akun** | ✅ Semua | ✅ Di sekolahnya | ❌ | ❌ |

**Aturan Kunci:**
- **Tidak ada percakapan atau pengumuman antar-murid (inter-student)** — backend memverifikasi role setiap penerima
- Murid yang mengirim pesan 1-on-1 (`target_role: "guru"` atau `"admin_sekolah"`) akan diverifikasi: setiap `recipient_id` harus memiliki role guru/admin_sekolah/super_admin
- Conversations API menyaring (skip) percakapan antar-murid untuk user role `murid` di server-side
- Broadcast (`target_role`) tidak bisa `"murid"` jika sender `murid` — di-restrict di `api_send_broadcast`

## UU PDP & PSE Kominfo Compliance Checklist

| Aspek | Status | Lokasi |
|-------|--------|--------|
| **Kebijakan Privasi** (Pasal 5, 14, 19) | ✅ 13 pasal lengkap (Eksplisit: NO birth date, NO sensitive data) | `/privacy`, `templates/compliance/privacy.html` |
| **Syarat & Ketentuan** | ✅ 11 pasal lengkap (Eksplisit: NO birth date) + RBAC matrix table | `/terms`, `templates/compliance/terms.html` |
| **Persetujuan Eksplisit** (Pasal 6) | ✅ Consent checkbox di registrasi | `templates/auth/register.html`, `routes/auth.py` |
| **Tujuan Pemrosesan** (Pasal 7) | ✅ Didokumentasikan di halaman privasi | `privacy.html §3` |
| **Penarikan Persetujuan** (Pasal 11) | ✅ Dijelaskan mekanisme penarikan + konsekuensi | `privacy.html §6` |
| **Hak Subjek Data** (Pasal 8-15) | ✅ Panel hak di settings + privacy page | `student/settings.html`, `privacy.html §5` |
| **Keputusan Otomatis/Profiling** (Pasal 21) | ✅ Diungkap (anti-cheat, auto-correct, weighted scoring) | `privacy.html §7` |
| **Data Sensitif** (Pasal 26) | ✅ Eksplisit: tidak mengumpulkan data sensitif (agama, biometrik, kesehatan, politik) | `privacy.html §2` |
| **Hak Akses & Portabilitas** | ✅ Ekspor data JSON via API | `api.py: /api/account/export-data` |
| **Hak Penghapusan** | ✅ Deletion request flow dengan admin approval, tenggang 90 hari | `api.py: /api/account/delete-request` |
| **Retensi Data** (Pasal 16) | ✅ Jadwal auto-purge + periode ditampilkan ke user | `data_retention_service.py`, `student/settings.html` |
| **Keamanan Data** (Pasal 17-18) | ✅ CSP, X-Frame-Options, X-Content-Type-Options, HTTPS, bcrypt, RBAC, backup, anomaly detection | `__init__.py` after_request handler |
| **Kewajiban Pengendali Data** (Pasal 20-24) | ✅ DPO configurable via super admin, respon 7 hari | `super_admin/privacy_settings.html` |
| **Transfer Lintas Batas** (Pasal 27) | ✅ Diungkapkan (Supabase Singapura) | `privacy.html §8` |
| **Pelanggaran Data** (Pasal 30-32) | ✅ Kebijakan notifikasi 14 hari + lapor ke PSE Kominfo | `privacy.html §10` |
| **Penyelesaian Sengketa** | ✅ Musyawarah → Mediasi 30 hari → Pengadilan RI | `terms.html §9` |
| **LocalStorage Consent** | ✅ Banner persetujuan pada kunjungan pertama | `base.html`, cookie banner |
| **DPO Contact** | ✅ Dapat dikonfigurasi via super admin, tampil di privacy + terms + footer | `system_settings key: dpo_contact` |
| **PSE Registration Number** | ✅ Dapat dikonfigurasi, ditampilkan di footer + terms | `system_settings key: pse_reg_number` |
| **RBAC Pesan** | ✅ 3-layer inter-student restriction + full matrix di terms | `api.py`, `api_conversations`, `api_send_broadcast`, `terms.html §5` |
| **Cookie/LocalStorage Banner** | ✅ Muncul sekali, disimpan di localStorage | `base.html` |
| **Email Kontak** | ✅ scangrade9@gmail.com, 7 hari kerja respon | `privacy.html §13`, `terms.html §11` |

## Progress
### Done (Latest)
- **Kartu fasilitas di landing page kini punya bukti, seperti klaim kinerjanya**: kartu pertama dulu berbunyi **"3 Tipe Soal — MCQ, Esai Teks (paragraph), Esai Canvas"** padahal `question_types.PICKER_TYPES` sudah berisi **enam** tipe berdiri sendiri — jadi halaman itu menjual setengah produknya *dan* menyebut `essay_text`, satu-satunya tipe yang sengaja tidak lagi ditawarkan builder. Sekarang jumlah **dan** namanya dihitung dari registry (`q_vocabulary()['picker']`), jadi kartu tidak bisa lagi berbeda dari aplikasi tempat ia menyalin; setiap kartu juga menyebut artefak yang membuktikannya (`data-facility`). `tests/unit/test_landing_facilities.py` menjaga tiga arah: kartu tanpa artefak, artefak tanpa kartu, dan angka/nama yang bukan milik registry — plus bukti bahwa empat provider AI, Midtrans, alat ukur, anti-cheat, ekspor XLSX/PDF, dan isolasi sekolah benar-benar ada di kode. 23 tes, mutasi 12/12. Dua temuan saat memverifikasi: (1) pola bukti versi pertama menggabungkan syaratnya jadi satu regex dengan lookahead `.*`, yang **kuadratik saat gagal** — dua mutasi yang menghapus `max_violations` dan `protractor` menggantung lewat batas 150 detik alih-alih melaporkan gagal, dan gate yang menggantung lebih buruk daripada gate yang merah; tiap pola kini jadi pencarian linear terpisah dan ada tes yang melarang `.*`/`.+` kembali; (2) harness mutasi versi pertama menghitung timeout sebagai "tertangkap", yang justru menutupi temuan itu.
- **Demo settings: order and visibility are separate questions, and the pages are held to the filter**: `/super-admin/demo-settings` can move a row (▲▼) and that order now drives the card stack on `/demo` and the tutorial row on the landing page. "Which ones, in what order" looks like one question and is two: the **settings page** must still list a row that is switched off — it is where you switch it back on — while the **landing page** and `/demo` must not draw it. Four hand-written `{% if %}`s per page answered it four times each, and they had already drifted (`demo_tutorial_guru` read by the landing page, `demo_guru` by `/demo`). `app/services/demo_settings.py` is now the only answer, the pages loop over `demo_items(...)`/`demo_order(...)`, and the guard is relational rather than a snapshot: for eight blob shapes (empty, all-off, sparse, a key from 2019, a jsonb value that arrives as a JSON *string*) the set a page draws must equal what the filter offers, in that order. Three defects surfaced while verifying: `demo_order` was called by the template but defined nowhere (the page 500s), the checkboxes opened blank while the landing page showed every button (now seeded from `effective_flags`, so what is ticked is what a visitor sees), and the arrow `<button>`s had no `type="button"` — inside a form that is a submit, so moving a row would also save. 48 tests, mutations 14/14. The production blob (nine flags, no order key) renders an **identical** visible set before and after: the feature is purely additive. The master `demo_tutorial` checkbox clears the three sub-flags on change; server-side an explicit sub-flag wins over the master, and a sparse blob follows it — all three cases measured live.
- **The OMR bench is usable on a phone, and the tab bar stops lying about the 44px floor**: `/super-admin/omr-test` was measured at 320/375/768/1024/1440/1920 with the page's own code driving it into its post-run state (24-row batch, three charts drawn). Four defects, each found by measurement rather than reading: the three canvases ran to **5 400–8 300px tall** (an unsized parent *is* the canvas with `maintainAspectRatio: false`, so every resize added a canvas height again); a file input plus a `shrink-0` button put the button's right edge at **374px on a 320px screen** (a native file input cannot shrink below its own text) and pushed the page **124px sideways**; the metric pills' `grid-cols-3` put a 79px NISN inside a 41px content box; and the batch dropdown was filled from `/api/exams/list`, an endpoint **no blueprint defines** — the 404 died in a `.catch()`, so no batch could ever be graded and "Rata-rata Skor" could never leave "-". The list is now rendered by the route, **retried** (the read failed 4 of 10 identical calls) and says so out loud when it cannot be read (`exams is None` — the query — is not `[]` — the data). Measured after: **no page overflow at any of the six widths**, charts a steady 254/309/326x160, dropdown carrying the real exams.
- **The bottom tab bar on a 320px phone**: six labelled items measure 64 / 47 / 36 / 78 / 60 / 36 there, because each is `flex-1` with `min-width: auto` — so the two shortest labels were crushed to **36px**, under the 44px the app holds every other control to and close enough to their neighbours that a thumb lands on the wrong tab. Under 360px the labels are now hidden *from the eye only* (`clip-path`, so a screen reader still hears them) with a floor in **both** axes; measured again: **53x44 each, 45px bar, no overflow**, and the labels are back at 360px where all six fit. Guarded in `test_mobile_layout.py`, mutation-checked 6/6 — including the two ways it can go quiet: a wrapper `<div>` between `> a` and `> span`, and the boundary drifting off 359px.
- **OMR, phone-photo power**: the scanner no longer needs a flatbed — measured **0 wrong and 0 invented answers across 57 tests**, including every page of every committed LJK format read back from the file on disk (perspective, tilt, blur, lighting gradient, JPEG). Print-ready sheets for 20/40/50/100/160 questions are in `docs/ljk/` with a printing guide; `test_scripts/check_omr_photo.py` reads a real photograph and explains itself. A silent defect that invented answers for questions not in the picture on any exam over 80 questions was found and fixed (see the multi-page bullet under Constraints).
- **UU PDP compliance pages**: `/privacy` (13 sections), `/terms` (11 sections with RBAC matrix) with `content_noauth` block for unauthenticated access — EYD-corrected, PSE Kominfo + UU PDP compliant
- **Privacy policy expanded**: Added Penarikan Persetujuan (§6), Keputusan Otomatis/Profiling (§7), Data Sensitif disclosure (§2 explicit Pasal 26 UU PDP — NO birth date, NO sensitive data)
- **Terms updated with RBAC matrix**: Full communication RBAC table in §5 matching AGENTS.md + dispute resolution (§9: musyawarah → mediasi 30 hari → pengadilan)
- **Teks diperbaiki EYD**: Seluruh dokumen compliance menggunakan bahasa Indonesia baku sesuai EYD
- **Contact email updated** to `scangrade9@gmail.com` with 7-day response policy
- **Security headers added**: CSP (`frame-ancestors 'self'`), `X-Content-Type-Options: nosniff`, `X-Frame-Options: SAMEORIGIN`
- **DPO/PSE configuration**: Super admin page at `/super-admin/privacy-settings` with save API
- **Footer**: Dynamic DPO/PSE display from system_settings via `/api/public/privacy-info`
- **Cookie/localStorage consent banner**: Fixed bottom bar on first visit
- **Login page**: Privacy/Terms links added
- **Student settings**: Rights panel (8 UU PDP rights), retention data card, simplified PDP consent checkbox
- **Parental consent API**: `POST /student/settings/pdp-update` with age validation (simplified to only pdp_agreed)
- **Consent at registration**: Required checkbox with `consent_at` timestamp
- **Inter-student restriction**: 3-layer (send-time role verification, list-time filter, FE tab restriction)
- **Data export + deletion**: Full API endpoints with RBAC verification
- **Auto-purge scheduler**: 24h data retention cleanup

### In Progress
- Many exams (Sejarah, Agama, etc.) have `None` answer keys — teacher must set correct answers via UI before scores appear
- All 3 pending migrations (002, 003, 011, 013) executed in Supabase SQL Editor — schema now matches code expectations

### Done (Batch 2026-07-16 — Major Bug Hunt)
- **Fixed `student_auto_save` no-op** (api.py): was accepting data and returning success without saving — now properly persists to submissions table
- **Fixed `NameError: ValidationError`** (exam.py): added missing import, upload PDF kosong no longer crashes
- **Fixed `NameError: app.logger`** (api.py): changed to `current_app.logger`
- **Fixed NISN login** (auth.py): added fallback to `auth.admin.list_users()` when `profiles.nisn` column query fails
- **Fixed Midtrans webhook security** (webhook.py): added HMAC-SHA512 signature verification; Fonnte token check
- **Fixed `/debug/exam` exposure** (__init__.py): added `@login_required` + school access check
- **Fixed CSRF bypass** (csrf.py): no longer "allow for now" when session has no token — now properly rejects
- **Fixed answer key exposure** (exam.py + student.py): stripped from API responses and HTML templates for non-teachers
- **Fixed essay weight = 0 in submit_exam** (student.py): added proper 70/30 MCQ/essay split matching `_recalculate_scores`
- **Fixed double submit race condition** (student.py): checks for existing submission before insert
- **Fixed blank page in answer sheet PDF** (answer_sheet_generator.py): removed extra `c.showPage()` after loop
- **Fixed MCQ comparison in exports** (export_service.py): handles multi-value `["A","B"]` and `"bonus"` keys
- **Fixed analytics scores** (analytics_service.py): uses `final_score` instead of `score`
- **Fixed scan_save grading** (api.py): handles dict-format MCQ answers + multi-value/bonus keys; added school access check; added status check (don't overwrite graded/published)
- **Fixed create/update/delete exam** (exam.py): added field whitelist, ownership check, teacher_id enforcement
- **Fixed publish routes** (teacher.py+publish.py): added `@require_school_access`; `/publish/` was a stub, now actually publishes
- **Fixed admin_sekolah logic** (teacher.py): removed `not exam_ids AND` condition — now always shows school exams
- **Fixed sync-draft race condition** (api.py): checks status before update — never overwrites submitted/graded/published; proper error logging instead of `except: pass`
- **Fixed memory leaks** (api.py+__init__.py): `_sync_last`/`_sync_locks` now clean up stale entries every 10 min; `_demo_cache` replaced with `g._demo_settings` (request-scoped)
- **Fixed timer reset on F5** (take_exam.html): `started_at` persisted server-side; `timeLeft` calculated from elapsed time
- **Fixed submit button loading state** (take_exam.html): added `submitting` prop, spinner, double-click prevention
- **Added CSP headers** (__init__.py): `Content-Security-Policy` added to all responses
- **Fixed OMR deskew memory explosion** (omr_service.py): replaced `np.column_stack(np.where(...))` with Canny edge + Hough lines
- **Fixed `get_whatsapp_number` no caching** (__init__.py): now cached per-request via `g._whatsapp_number`
- **Created migration 013** (supabase/migrations/013_fix_missing_columns.sql): adds `profiles.school_id`, `submissions.started_at`, `schools` table, unique constraint `(student_id, exam_id)`

### Blocked
- Exam scoring shows 0 for students when answer keys are `None` (teacher has not set correct answers)


## Key Decisions
- **Two separate Supabase clients** to avoid service-key client corruption from auth operations
- **Auto-create profile** in register route, not DB trigger
- **Weighted scoring**: MCQ% + Essay% (total=100), distributed equally within each group — fallback to 70/30 when `question_weights` is empty
- **Essay types simplified** to single "Esai" toggle — canvas drawing + text boxes combined on PDF
- **Anti-cheat graduated penalty**: 1st=warning(0), 2nd=-base, 3rd=-2×base, 4th+=-3×base per violation; cap at 100
- **Tool SVGs** always `pointer-events: none` — drag/rotate from toolbar control bar only
- **PDF generation**: `xhtml2pdf` over WeasyPrint (no GTK/Pango on Windows), over pdfkit (needs wkhtmltopdf)
- **Server-side compositing for PDF**: Pillow merges all layers into flat `<img>` — xhtml2pdf doesn't support `position: absolute`
- **NISN**: fill from LEFT if < 10 digits (most significant first, trailing cells empty) — standard OMR convention
- **MCQ answer format**: Use dict `{answer, pages}` only when canvas/text data exists; keep string for backward compatibility
- **Inter-student restriction**: Three-layer defense — (1) `api_send_broadcast` memverifikasi role setiap recipient, (2) `api_conversations` server-side skip conv antar-murid untuk role murid, (3) FE hanya menampilkan guru di "Pesan Guru" tab
- **Tailwind CSS rebuild**: `npm run css:build` after every template change; `npm run css:watch` for dev auto-rebuild
- **CSP/security headers**: Added to `_register_performance_headers` after_request for all responses
- **Content blocks**: Privacy/terms templates define BOTH `block content` (authenticated) and `block content_noauth` (public)
- **No birth date collected**: Privacy policy explicitly states NO birth date collected or stored. Terms also state no birth date required for registration. Student PDP settings simplified to consent checkbox only.
- **Privacy policy expanded**: 13 sections covering UU PDP + PSE Kominfo — added Penarikan Persetujuan (§6), Keputusan Otomatis/Profiling (§7), Data Sensitif (§2 explicit Pasal 26 disclosure).
- **Terms updated with RBAC matrix**: Full communication RBAC table in §5 matching AGENTS.md matrix, plus dispute resolution (§9: musyawarah → mediasi 30 hari → pengadilan).
- **Terms route now passes dpo_contact**: Both `/privacy` and `/terms` fetch DPO from system_settings.
- **Title optional**: Auto-generated from first 50 chars of message. Users no longer need to think of a title — just type and send.
- **Contacts-based sidebar**: Sidebar shows ALL people the user can message (RBAC-filtered, via `/api/broadcast/contacts`), not just existing conversations. Click any contact → if conversation exists, open it; if not, start new chat directly. No "Pesan Baru" popup needed. `pendingRecipient` drives the new-chat flow.
- **Always-visible input bar**: Shows when `activeConv || pendingRecipient`; no compose form replaces the chat view.
- **WhatsApp exact match**: Sent bubble green `#dcf8c6` with `text-[#111b21]` (not blue/primary). Timestamp + double-checks inside bubble at bottom-right (not outside). Check mark colors match WhatsApp: gray `#8696a0` → blue `#53bdeb` for read (last msg).
- **Conversation before notification**: `api_send_broadcast` creates/updates conversation first, then inserts notification WITH `conversation_id` — no separate UPDATE needed, no race condition.
- **Graceful fallback for missing columns**: All migration-021-specific features (unsend, hide, soft-delete) try the new columns first; if they don't exist, fall back gracefully (e.g., unsend always sets `message="Pesan telah hapus"` even if `is_deleted` column is missing).
- **Messages area guarded by `x-if="activeConv"`**: Prevents Alpine errors when `pendingRecipient` is set but `activeConv` is null.

## Anti-Cheat System — Panduan per Role

### Guru (Pembuat Ujian)
| Fitur | Deskripsi | Letak |
|-------|-----------|-------|
| **Blokir Screenshot** | Mencegah PrintScreen via `navigator.clipboard.writeText('')` | Form ujian → Pengaturan Anti-Cheat |
| **Blokir Copy-Paste** | Mencegah copy/paste/cut via `e.preventDefault()` | Form ujian → Pengaturan Anti-Cheat |
| **Blokir Klik Kanan** | Mencegah context menu via `e.preventDefault()` | Form ujian → Pengaturan Anti-Cheat |
| **Wajib Fullscreen** | Ujian hanya bisa dikerjakan dalam layar penuh. Keluar dari layar penuh (Esc), me-restore jendela, atau minimize → overlay memblokir ujian sampai kembali ke layar penuh, dan tercatat sebagai `fullscreen_exit` + dihitung di tangga penalti | Form ujian → Pengaturan Anti-Cheat |
| **Watermark Nama** | Menampilkan nama siswa sebagai watermark di seluruh halaman ujian | Form ujian → Pengaturan Anti-Cheat |
| **Penalti per Pelanggaran** | Base penalti (default 5 poin) — 1st=warning, 2nd=-base, 3rd=-2×base, 4th+=-3×base | Form ujian → Pengaturan Anti-Cheat |
| **Maks Pelanggaran** | Jumlah pelanggaran sebelum auto-submit (default 5) | Form ujian → Pengaturan Anti-Cheat |

**Catatan Penting:**
- `block_screenshot` sudah diperbaiki — sekarang gate-nya ke `block_screenshot`, bukan `block_copy_paste`
- Semua pelanggaran tercatat di tabel `violation_logs` + dihitung server-side di `submit_exam()`
- Yang **dihitung** ke tangga penalti hanya `tab_switch` dan `fullscreen_exit` (`PENALIZED_VIOLATION_TYPES` di `anti_cheat_service.py`); tipe lain (mis. `blur`) hanya dicatat
- Fullscreen adalah state yang **dilaporkan tepat** oleh browser (`document.fullscreenElement`). "Jendela maximized" tidak bisa dideteksi halaman web (tidak ada API-nya; `outerWidth/outerHeight` berubah oleh zoom), jadi fullscreen yang diwajibkan
- Jika browser tidak punya Fullscreen API (Safari iPhone) atau policy mematikannya (`fullscreenEnabled === false`), pemeriksaan ini **berhenti sendiri** — siswa tidak digagalkan karena hal yang tidak bisa ia perbaiki
- Timer ujian divalidasi server-side via `student_sync_draft()` — jika ada mismatch >300 detik antar device, client di-reject
- Jika siswa mematikan JavaScript, server tetap hitung penalti dari violation_logs yang sudah tercatat

### Super Admin / Admin Sekolah
| Fitur | Deskripsi | Letak |
|-------|-----------|-------|
| **Lihat Flag Kecurangan** | Submission dengan `_flags` berisi `suspicious_speed` atau `device_mismatch` akan terlihat di detail submission | Detail submission → `answers._flags` |
| **CSP Header** | Content-Security-Policy set on every response — currently only `frame-ancestors 'self'`. It does **not** restrict `script-src`/`style-src` (no such directive is emitted), so third-party scripts are not blocked by CSP; the X-Content-Type-Options and X-Frame-Options headers beside it are real | `__init__.py` `add_performance_headers` |
| **Speed Analysis** | Jika >5 MCQ dijawab dalam <1.5 detik/soal, submission di-flag suspicious | Server-side di `submit_exam()` |
| **Device Mismatch** | Jika IP atau User-Agent berubah antara first sync dan submit, submission di-flag | Server-side di `submit_exam()` |
| **Timer Reconciliation** | Server memvalidasi `started_at` — jika selisih >300 detik antar device, sync di-reject (409) | `api.py` → `student_sync_draft()` |

### Murid (Peserta Ujian)
| Aturan | Konsekuensi |
|--------|-------------|
| Pindah tab / buka aplikasi lain (visibilitychange) | 1st = **PERINGATAN**, 2nd = **-base poin**, 3rd = **-2×base**, 4th+ = **-3×base** |
| Keluar layar penuh via Esc (`fullscreen_exit`) | Overlay memblokir ujian + masuk tangga penalti yang sama (1st = PERINGATAN). Waktu ujian tetap berjalan |
| Restore-down / perkecil jendela | Terdeteksi (state fullscreen disampel tiap 2 detik, tidak hanya lewat event) → overlay memblokir |
| Minimize | `visibilitychange` → tangga penalti (dihitung 1× saja, tidak dobel dengan overlay) |
| Copy/paste/cut | Diblokir — `e.preventDefault()` |
| Klik kanan | Diblokir — `e.preventDefault()` |
| PrintScreen | Diblokir (jika guru mengaktifkan `block_screenshot`) |
| JavaScript dimatikan | Server tetap punya catatan violation_logs + hitung penalti saat submit |
| Buka 2 tab | Timer reconciliation — server reject jika mismatch >300 detik |
| Kerjakan dari device lain | Device mismatch terdeteksi saat submit (IP/UA berbeda) |
| Menjawab terlalu cepat | Speed analysis — jika <1.5 detik/soal untuk >5 MCQ, submission di-flag |
| Mencapai maks pelanggaran | **Ujian otomatis dikumpulkan (auto-submit)** |

## Next Steps
1. **Deploy to VPS**: `cd /opt/scangrade && git pull origin main && sudo systemctl restart scangrade`
2. **Run migration 021** in Supabase SQL Editor (`supabase/migrations/021_conversation_crud.sql`) — unblocks unsend/hide/soft-delete CRUD
3. **Verify all 4 RBAC**: contacts list, direct chat, edit, unsend, sembunyikan, hapus, badge read state
4. **Configure DPO/PSE**: Login as super_admin → `/super-admin/privacy-settings` → fill DPO contact, PSE reg number, data controller info
5. **Register as PSE Kominfo** (operational, not code)
6. **Appoint DPO** (operational, not code) and set contact in system_settings

## Key Files (Notifications)
- `app/routes/api.py`: All conversation/broadcast CRUD + title auto-generation + conv-before-notif insert + graceful migration-021 fallbacks + `last_msg_preview`/`unread_count` + `/broadcast/contacts` endpoint.
- `supabase/migrations/021_conversation_crud.sql`: Unsend, soft-delete, message_hides support (must run in SQL Editor).
- `app/templates/teacher/notifications.html`: 3 tabs + contacts sidebar + CRUD + broadcast form.
- `app/templates/student/notifications.html`: 2 tabs + contacts sidebar.
- `app/templates/admin_sekolah/notifications.html`: 5 tabs + contacts sidebar + CRUD + broadcast + deletion requests.
- `app/templates/super_admin/notifications.html`: 5 tabs + contacts sidebar + CRUD + broadcast + privacy settings.

## Critical Context
- Alpine.js v3.14.8 — `el.__x` does NOT exist. Must use `QUESTION_INSTANCES` global map pattern. NOTE: admin templates use `Alpine.raw(root).__x.$data` for import forms.
- `xhtml2pdf` does NOT support: `display: flex`, `position: absolute/relative`, `border-radius` (limited), `gap` — use tables for layout, floats for positioning
- PDF page URLs are local paths like `/static/uploads/exams/<uuid>/page_001.png` — load via `os.path.join(app.static_folder, ...)` not HTTP
- Student answer JSON (MCQ with canvas): `{"0": {"answer": "A", "pages": {"0": {"canvas": "data:image/png;...", "textBoxes": [...]}}}}` — old format `{"0": "A"}` still supported
- Answer key JSON: `{"0": "A", "1": ["A","B"], "2": "bonus", "3": "essay"}` — `None` keys cause score 0
- `question_weights` + anti-cheat columns now exist in DB (migrations 003 + 011 executed)
- `submissions.is_hidden` column exists (migration 003 executed)
- `profiles` now has `nisn`, `nis`, `class_id`, `school_id`, `tz_offset` (migrations 002 + 013 executed)
- `schools` table now exists (migration 013 executed)
- Violation `sendBeacon` Blob: `new Blob([JSON.stringify([{...}])], {type: 'application/json'})` — must have correct bracket nesting
- Grade detail form: pressing Enter in score/comment inputs submits the HTML `<form>` and reloads page — `@keydown.enter.prevent=""` added to both inputs
- NISN: jika < 10 digit, isi dari kiri (most significant digit pertama), trailing cells kosong
- `final_score` dihitung sebagai `max(0, score - penalty)` saat submit; untuk submission lama di-backfill via script
- Jawaban guru untuk komentar: mulai dari huruf besar setelah titik dan spasi — `calcFinal()` panggil 50ms setelah input berubah
- `_saveCurrentPage()` dan `getAnswersLight()`/`getAnswersWithCanvas()` sudah diupdate untuk MCQ — canvas di `ec-canvas-{i}` di-init melalui `initEcCanvas(i)`
- Essay section's SVG overlay drag/rotate handles: use `startToolRotate(i,'ruler',$event)` — 3-arg form (was `startToolRotate('ruler',$event)` broken)

## Key Files (Compliance)
- `app/routes/public.py`: `/privacy` and `/terms` routes with DPO fetch
- `app/templates/compliance/privacy.html`: Full Kebijakan Privasi — 13 sections (both `block content` and `block content_noauth`)
- `app/templates/compliance/terms.html`: Full Syarat & Ketentuan — 11 sections with RBAC matrix (both `block content` and `block content_noauth`)
- `app/templates/super_admin/privacy_settings.html`: DPO/PSE/controller configuration
- `app/routes/super_admin.py`: `/privacy-settings` route + save API
- `app/routes/student.py`: PDP settings endpoint (simplified — only pdp_agreed)
- `app/templates/student/settings.html`: Rights panel, retention data card, simplified PDP consent checkbox
- `app/__init__.py`: CSP + security headers in after_request
- `app/routes/api.py`: `/api/public/privacy-info`, broadcast RBAC, conversation filtering
- `app/templates/auth/login.html`: Privacy/Terms links
- `app/templates/auth/register.html`: Consent checkbox
- `app/templates/base.html`: Cookie banner, DPO/PSE in footer

- **A mark scheme is per question *type*, and the paper totals exactly 100 — but a paper that already has marks keeps them.** The two-pool model (objective% / essay%, split equally inside each pool) could not express a scheme a teacher writes down: every objective question got the same marks whether it was a one-line true/false or a four-pair matching item, three essay questions behind 70% were 23.33 each so the paper totalled **99.99**, and every objective question was all-or-nothing — a four-pair matching question answered three ways right earned **nothing**. `app/services/mark_scheme.py` now prices each *type* (`{mcq: 1, true_false: 1, match: 3, drag_drop: 3, essay: 10}` by default, teacher-editable) and scales the paper to exactly 100 by **largest remainder in whole 0.1 steps**: rounding each share on its own is what produces 99.9/100.1, so the leftover steps go to the values that lost the most. `question_types.part_factor` awards the share of a matching question's *pairs* (or a drag & drop's *positions*) that are right, compared against the key's own items so a right pairing given twice with a distractor cannot earn twice. **The scheme rides inside `question_weights` under `_scheme`** (`question_types.SCHEME_KEY`), which is what makes the freeze possible rather than aspirational: a result is *recomputed from stored answers* at publish time and whenever marks are recalculated, so a rule applied to every paper would move marks already in students' hands. An exam with no `_scheme` is scored by the shipped expression — asserted against that expression written out in the test, not described — and awards no part-marks; the builder only writes a scheme when a teacher uses one, so opening and saving an old exam changes nothing. `earned_points` reads the flag off the weights it already receives, so all four scoring sites became scheme-aware with no signature change. The builder's per-type editor carries a **JavaScript copy** of the arithmetic so the total moves while a teacher types — and it is only ever a preview: both exam-saving routes call `_apply_mark_scheme`, which recomputes the marks in Python from the scheme and ignores what the page posted, so a stale page or a form edited in devtools shows a wrong total rather than writing one. A scheme stored by hand in the console that names only some types keeps the rest (`weights_for` merges the stored one in; without that merge an unmentioned type silently falls back to its default). The marking page, the student's result and the printed sheet all show *earned of total* — through `q_part_factor`/`q_partial_credit`, which are the scoring functions themselves exposed as Jinja globals, checked by identity — and a partly-right answer is amber, not red. Two things the work found: `teacher/grade_detail.html` **pins Indonesian by design**, so a `t()` pair added there is dead copy the language gate refuses (it is plain text now); and the gate's own floor had to be re-recorded because `student/result_detail.html` gained a pair. Guards: `tests/unit/test_mark_scheme.py` (37 tests — exactness over 1..80 questions × 5 mark values, the 99.99 arithmetic stated as arithmetic, part-marks including distractors and doubles, the freeze matrix compared against the shipped expression, the route ignoring the page's numbers, the console-partial merge, the pages routing through the scoring functions, and the Python and JavaScript implementations run over the same inputs) — mutation-checked, **7/7 injected defects caught**, the first run having missed one (`weights_for`'s merge) until the test used a *partial* stored scheme, which is the only shape that exercises it.

- **One divisor for a partly keyed paper: the paper's objective questions, not the answers the key happens to hold.** Six places computed a submission's auto-graded score in four ways, and two of them — the scanner's save routes and the Celery OMR task — divided by `objective_key_count`, so a teacher who had keyed **2 of 10** and scanned one sheet was shown `2 / 2 = 100%` for a paper that was two-tenths marked, while the same pupil's online submission stored the *weighted* objective marks and the teacher's own "recalculate scores" wrote `2 / 10 = 20%` to the same column: three numbers for one sitting, chosen by which button was pressed. `question_types.objective_result` is now the one rule — `score = correct / (objective questions on the paper) * 100`, an objective question with no key scored **wrong** (the safe direction, and what `_needs_answer_key` already told the teacher), so a partial key *depresses the ceiling* instead of inflating the score; `ObjectiveResult` carries `correct`, `out_of`, `keyed`, `unkeyed` and a `ceiling` property so a page can put the shortfall in a number. All six writers call it: `student.py` submit (whose `final_score` still comes from the **weighted** `earned` minus penalty, not the percentage — reading `score` there would have silently re-scaled every published final mark), `teacher._recalculate_scores`, `api.scan_save`, `api.scan_bulk_save`, `services.omr_tasks._run_omr` (which had to add `total_questions` to its `select`, because a column left out reads as absent and absent means "no questions"), and `super_admin.omr_test_batch`. Two defects fell out of the work: `scan_bulk_save` referenced `mcq_count` without ever assigning it — a **NameError** on the first sheet of every bulk import, hidden in the `except` that records it as a per-student error — and `max_score` held a question *count* in the scanner routes while `score` held a percentage, two units in one pair of columns. The teacher dashboard now tells the two key situations apart instead of staying silent about the worse one: `_needs_answer_key` (objective questions and the key answers **none**) keeps its "scores will be 0" card, and `_partial_answer_key` gets its own amber card naming the lowest ceiling among the affected exams; the scan screen and the OMR bench append "x/y questions have no key" to a score that looks wrong. Guards: `tests/unit/test_objective_score_rule.py` (27 tests — the rule's arithmetic including the unkeyed-is-wrong and essay-not-counted cases, a source-level guard on each of the six writers, no file outside `question_types.py` reaching for the keyed count, `max_score` being 100 in both scanner routes, and an AST check that `mcq_count` is bound before it is read, which a substring search could not state because the explanatory comment names it) — mutation-checked, **14/14 injected defects caught** (`.freebuff/mutate_objective_score.py`), and the i18n floors re-recorded because `teacher/dashboard.html`, `teacher/scan.html` and `super_admin/omr_test.html` gained pairs while gaining no leftovers.

- **A question type is a kind of question, not a flavour of multiple choice — and "ordering" was missing while its mechanic already shipped.** The builder told teachers *"choice, true/false, matching and drag & drop are auto-marked — **they all share this weight**"*, and the 70/30 pool model said the same thing in arithmetic: a true/false question beside fifty choice questions was worth a fiftieth of one choice question's marks, and **there was no way to say what a matching question was worth**. `OBJECTIVE_TYPES` is now documented as a statement about *who marks it* (the app, not the teacher) and not one about standing; the scheme gives **one row per type** — `(mcq, true_false, match, drag_drop, order, essay_canvas)` — and every page that renders a type name names it explicitly (`else` used to mean "Esai", so an ordering question would have been labelled an essay in the report card, the result page and its PDF, and "Susun Kata" in the grading page). The legacy pool editor still exists and says so on the panel: a paper marked before schemes existed has to be reproducible byte for byte, which is what keeps published marks where they are. **The missing type was `order` (`Mengurutkan` / Ordering)**: the app already graded a sequence — that is what `drag_drop` does — so the two share the key (`{"order": […]}`), the grader, the part-credit rule and one builder editor, and the teacher chooses between them by what a student *does* with it: drag & drop is *dragged into* a box, ordering is *ranked* (a numbered list with up/down, because a sentence is too wide to drag on a phone). `drag_drop`'s own label dropped the name it had borrowed — "Susun Kata" described ordering, not dragging. Part-credit covers ordering too: four items with three in place is three quarters, where the old rule called it zero. Because both types write the same shape, re-typing a question from one to the other keeps its answer, and every already-stored drag & drop question grades exactly as it did. Guards: `tests/unit/test_objective_types.py` (the ordering truth table, part-marks, the no-scheme freeze, the pupil's ranked control *and* its rank badge, the builder's one editor naming both kinds, a per-type scheme row, and the reported sentence banned from the pages) plus `tests/unit/test_mark_scheme.py`, which now lifts the builder's own `keyFor` of the Alpine object and **runs it against the grader's `normalise_key` for every pickable type** — the failure it exists for is an ordering question saved under the essay marker, which renders fine, keeps the teacher's sequence on screen, and scores every pupil zero. Mutation-checked **15/15 injected defects caught** — the first run caught 12, and three had to be *tightened* first: `normalise_key` was handed the stored shape (which every branch returns unchanged), the editor guard searched forwards and measured the *matching* editor next door, and the key check compared only Python to Python, so nothing executed the page's own `keyFor`. Verified end to end on the running app with a temporary exam and pupil (`E2E ordering`, created and deleted in a `finally`): five kinds with a scheme of `{mcq:1, true_false:1, match:2, drag_drop:3, order:3}` came back as marks per question `[10, 10, 20, 30, 30]` totalling exactly 100, the pupil's page was sent the items and not the sequence, and a sequence with two of three positions right scored **90**, one of three **80**, and a reversed one **80** — where all-or-nothing would have answered 70 to every partial attempt.

- **A student's drawing is on exactly one page, and three readers were each inventing a page.** The report was a teacher seeing one annotation on several pages of a student's essay. The stored answer proves it was never storage: submission `b053cf1e` (`Fisika`, 6 sheets) holds `answers['4'] = {'type': 'essay_canvas', 'pages': {'2': {'canvas': …}}}` — **one** key. Three readers were wrong, independently. (1) The teacher's marking view looked the drawing up as `pages[page-1] || pages[page]`, a tolerance for the neighbouring key, so a drawing made on page 3 was painted onto page 2 as well; the same tolerance sat on the text-box overlay (`page == pn+1 || page == pn`), and the student overlay canvas was never cleared before the early return, so a page the student skipped kept showing the last drawing for the rest of the session. (2) `routes/student.py`, `print/report_card.html` and `student/result_detail.html` each guessed the page as `p_idx if '0' in s_pages else p_idx - 1` — invisible when the student drew on page 1 (key `'0'` exists, the subtraction is skipped) and **wrong every other time**: the production submission was printed and displayed over **sheet 2** instead of sheet 3. (3) `result_detail.html` iterated `s_page_keys + ov_page_keys` while the block below it draws *both* maps for one key, so a page the student drew on **and** the teacher annotated was rendered twice in a row — which is what "the annotation is on several pages" looks like from the student's side; `print/report_card.html` already merged the two key sets, and the result page was brought into line. There is one convention and it is not a guess: `student/take_exam.html` writes `canvasData[i][this.page - 1]` and `saveOverlayForPage()` writes `GRADE_STATE[qIdx][this.page - 1]`, so both maps are **index-keyed**, and a survey of every stored submission found no key that could only have been a page number (a page-number reading would have to reach the page count for a drawing on the last sheet). Verified live against that submission, both states: **after**, the drawing is on sheet 3 in both the printed card and the student's result page, once; with production's rule put back and the server restarted, **both put it on sheet 2**. The teacher view now serves the one-page rule, `sgCanvasForPage`, whose `clearRect` sits *before* the early return. Guards: `tests/unit/test_grade_canvas_pages.py` (23 tests) — the rule is *executed* in node over the production shape, the Jinja merge is executed from the snippet extracted from the real template, and the source checks blank `{# … #}` first, because each file documents the expression it retired and a guard that reads commentary fails on its own documentation — mutation-checked, **7/7 injected defects caught** (the seventh anchor needed the harness to be line-ending aware: `student.py` is CRLF, and an anchor written with `\n` silently misses, which reads as a guard that does not bite).

- **`RemoteProtocolError: Server disconnected` is not only a threading story — it happens on a single read, and a page that swallows it lies.** Lines above record it under *concurrency* (the dev server's thread workers). Measured against the live project, though, **ten identical `exams` reads in a row failed four times** on one quiet process — rows, rows, *failed*, rows, rows, *failed*, *failed*, rows, *failed*, rows — because the Supabase HTTP client keeps connections alive and the server closes them; the library surfaces that on a query that is perfectly correct. On the OMR test bench that read filled the batch dropdown behind a bare `except Exception: pass`, so **one load in three showed a select holding only — Tanpa grading —** and nothing anywhere said why: on screen *“this bench has no exams”* and *“this bench could not ask”* are the same pixels, and the second one quietly made a batch run ungraded. `app.utils.helpers.read_with_retry` retries the transport (`httpx.TransportError`) and **not** `postgrest.APIError`, which is the server answering — a bad column, an RLS refusal — where a second attempt changes nothing; the last transport error still raises, so a caller can tell an unanswered read from no data. The route then passes **`None`** on failure and `[]` only when the list really is empty, and the page says `Daftar ujian gagal dimuat — jalankan tanpa grading`. Verified live: **10 loads, 6 options every time, 0 notices, min 6 max 6** (the same probe before the change listed 6 options on 10 of 12 loads). Two traps this work found: `{% if exams is None %}` is **not** Jinja — the test is lowercase `none`, and the Python spelling is a `TemplateRuntimeError`, i.e. a **500 on the very page whose job is to report a failed read** (caught on the first live load, invisible in a static read of the diff); and `{% for e in exams %}` over `None` raises rather than rendering, which is why the select stays `exams or []`. Guards: `tests/unit/test_transient_reads.py` (the retry, the growing wait, the non-retry of a server answer, the raised last error, the route's `None`, the notice's presence *and* its lowercase test, the `or []` in the loop).

- **A metric pill is sized by its own content, not by a column count.** The OMR bench's `grid grid-cols-3` metric strips measured a ten-digit NISN — **79px at 12px/700**, plus the pill's own 16px of padding — inside a **57px** column at 1024 and **77px** at 320: the number sat outside its coloured box at three of the six widths measured, because the card is 227px wide when the three tool cards are three-up at `lg`. The strips are `flex flex-wrap` with a `min-w-[6.5rem]` floor on every pill now, so the row wraps instead of overrunning and the layout no longer depends on how wide the card happens to be; the tool cards are `lg:grid-cols-2 xl:grid-cols-3` so a 1024—1279 window gets **352px** cards (measured) instead of 227, with the charts strip matching. Verified by driving the page's *own* code path — a stubbed `fetch` and real `File`s on the three inputs, not hand-written markup, which is how the first fixture measured its own class list while the page's was still broken — at 320/375/768/1024/1280/1920: `clipped 0` and `document.scrollWidth == viewport` at every width, all three canvases drawn and parent-sized, and every pill's content inside its box. Guards: `tests/unit/test_mobile_layout.py::TestTheOmrBenchFitsEveryDevice` (the strip is not a fixed column count, all seven pills carry a floor of at least 6rem — below the 5.94rem a ten-digit NISN needs here), mutation-checked **20/20** with the harness in `.freebuff/mutate_omr_bench.py`.

- **An empty `class_ids` meant "the whole school", and the list that offered an exam read a column it never asked for.** A pupil reported "Ujian ini tidak ditugaskan untuk kelas Anda." after clicking a card the app had just handed them, and two separate defects produced it. (1) `/student/exams` filtered its rows on `class_ids` while its `select("id,title,subject,question_types,total_questions,duration_minutes")` did not include that column, so `e.get("class_ids") or []` was `[]` for **every** exam and the rule `if not cids or student_class_id in cids` matched the whole school: rendered against production, a pupil in X-B was shown the papers for X-A and XI-A — **three cards, three refusals**, and a pupil in VII-A whose own exam *was* assigned saw it only because `not cids` happened to pass. (2) The guard read the same empty list as "open to the whole school", a documented reading, so an exam a teacher never assigned reached every class and a pupil with no class on file was offered exactly the unassigned papers. The rule is now one function, `exam_access.class_assignment_allows`, used by the exam list, the dashboard and the guard: assignment is a **positive** fact (the classes the teacher ticked, and nobody else), `class_ids` arrives as jsonb **or** a JSON string (`exam_class_ids` normalises both, and a bare string is not a list of classes), and a pupil with no class matches nothing. A guard test asserts the relation rather than the rule — *nothing the list offers may be refused by the door* — so the two halves cannot drift apart again whichever way the rule moves. Because assignment is now required, forgetting to tick a class makes a paper invisible, which the teacher has to be told: `_needs_class_assignment` (published **and** active, and unassigned) drives a rose card on the teacher dashboard and a `Tanpa kelas` badge on each exam card, with `class_ids` added to that route's column list — the lesson `answer_key` already taught, where a column left out of a select made a warning permanent. Both student queries now go through `read_with_retry`, because the harness caught the page answering **an empty list** on a dropped connection: browsing as a pupil, one of the renders in that run listed nothing at all where the school had three live exams, which is indistinguishable from "your class has nothing" and is the same defect `test_transient_reads` records on the OMR bench. Guards: `tests/unit/test_exam_assignment.py` (the query must select the column it filters on, checked for both student pages; only the own-class exam is offered; an unassigned exam to no class; no class is not a wildcard; another school never appears; the list-offers-nothing-the-door-refuses relation; the JSON-string case; the teacher's flag, its two rendering sites, and the card list rendered twice to prove the badge lands on exactly the unassigned card) and `test_exam_integrity.TestClassAssignment` (the predicate's truth table over every shape `class_ids` takes, and the door agreeing with it shape by shape) — mutation-checked, **8/8 injected defects caught**, including the two that only a *relation* catches: a list that keeps its own copy of the rule, and a warning list computed but never handed to the template. Verified against the real database in a single round trip (`.freebuff/probe_assign_e2e.py`, row created and deleted in a `finally`): a live exam with no class in school `b98a2360` was listed by **neither** of its two classed pupils (`no`, `no`), flagged as unassigned by `/teacher/exams`, and named by the teacher dashboard's warning — before the change the same probe showed **4 dead ends** across two pupils, every one of them a card leading to "Ujian ini tidak ditugaskan untuk kelas Anda." Two traps the probes paid for: **a write is never retried** — funnelling the insert through `read_with_retry` produced five identical exam rows from one dropped response, and cleanup had to be by title rather than by the id the insert returned; and the teacher dashboard itself answered its error page on a dropped read — `Server disconnected` on **2 of 5** loads while measuring, with a stack trace naming the exams select at `teacher.py` as the throw site — so that route's exams and submissions reads go through `read_with_retry` too, because the first page a teacher opens must not be a 500 the app already knows how to survive.

- **A measurement can be most of what it measures, and the first version of this one was.** Attributing a request's CPU between gunicorn, nginx, redis and the kernel needs per-process counters, and the only channel left when SSH is refused is the app itself — so `/metrics/processes` exists, and it is expensive: enumerating ~200 processes with `psutil` and rendering four metrics for each costs **61.6 ms of the app's CPU per sample**. At 1 Hz on an idle box that was *most of the box's busy time* (7.9% busy, the sampler the largest single contributor), which is the sort of observer effect that quietly becomes the finding. So the load phase samples every three seconds and every figure has the idle-measured cost subtracted — and the static phases report ~0 for gunicorn precisely *because* their whole gunicorn reading was the instrument. Two smaller traps cost a run each: `Connection: close` forces a fresh handshake, but building a `requests.Session` per request rebuilds the SSL *context*, so the client became the bottleneck at 9 req/s and the number described this laptop rather than the server; and with a 105 KB asset a fresh connection spends its time in TCP slow-start (301 ms wall against 22 ms reused), so a handshake can only be priced over a small payload. What came out the far side: at the published rung a request costs ~25 ms of CPU and **~85% of it is gunicorn rendering the page** (20.9 ms), the box was only 54% busy at 22 req/s so the CPU cost extrapolates to ~41 req/s, and a fresh connection adds 4.3 ms of nginx CPU plus ~5 ms of kernel work *no process is charged for*. Artifact: `docs/measurements/cpu-attribution-050.txt`.

- **One page had two URLs, and the device preview is where it showed.** `/tools/device-preview` derives its list from the URL map, so the panel that existed before this app served more than one school (`/admin/*`, which reads `profiles`, `classes` and `exams` with no school filter and renders the `admin/*.html` layout) sat beside the prefix that replaced it: the school-admin section listed `/admin/dashboard` **and** `/admin-sekolah/dashboard`, and the same for students, teachers, classes and messages — two buttons each, separated only by a `title` tooltip, which the phone the preview exists to check cannot hover. Both halves stayed routable because nothing had ever decided which one was canonical. Nine pages now answer **308** to the page that owns the content (`/admin-sekolah/*` for what is school-scoped, `/super-admin/*` for what reads the whole platform), the legacy handlers are deleted, and the mapping lives in exactly one place — `app/utils/legacy_urls.py` — which `create_app` registers rules out of *and* `device_preview.skip_reason` reads, so a path cannot be redirected and still be advertised as a page. **308 rather than 301 is the point**: it preserves the method, so a bookmarked form POST is re-sent to the page that owns it instead of becoming a GET that drops its body, and the query string is carried over for the same reason (`?page=3` dropped lands the reader on a page that looks fine and is not). The section had a *second* source of duplicates, found only after the first was fixed and the preview still showed two Imports: `/students/import` is a **guru-only** CSV page whose blueprint happens to hang off `/students/`, and it was filed with the school admin — so it now sits in the Teacher section, where `_unique_labels` no longer has to lengthen either name. Two things deliberately left alone and worth naming: the legacy *writes* stayed (`/admin/school` is a GET-308 with its POST still routed, because the old form's field names are not the new form's — a re-posted body would land on a route that cannot read it), and so did the two legacy importers, which `test_legacy_importers.py` pins for their orphan-account rollback. What that leaves is a real finding rather than a tidy-up: `/admin/classes/create` defaults `school_id` to **1** and `/admin/{students,teachers,classes}/<id>/delete` are `@admin_required` with no school filter, so a school admin can still call them by hand against another school's rows — the pages that used to link them are gone, not the endpoints. Also updated: the login/index redirect maps went to their role's own dashboard instead of to `/admin/dashboard` (which is now a redirect, and was a needless hop for `super_admin`, whose real dashboard is `/super-admin/dashboard`). Guards: `tests/unit/test_canonical_admin_urls.py` (every moved URL is routed and answers exactly 308 to a target that exists, no target is itself a legacy URL, every target sits under a role prefix, the query string survives, the POST that stayed is still routed, no page is listed by the preview under both `/admin/` and a role prefix — compared by last segment, which is what made them look like one page to a reader — the school-admin section holds nothing from another role's prefix, and a source rule that the preview *reads* the table instead of keeping a copy) — mutation-checked, **5/5 injected defects caught** (the fifth needed two edits at once, and the harness — `.freebuff/mutate_canonical_admin.py` — had to capture every original before the first write: backing up mid-mutation restored the mutated file and the first run reported a MISSED guard that was really a broken harness).

- **The dropped connection was fixed at three call sites, so it kept happening — and on the deploy it rolled healthy releases back.** `RemoteProtocolError: Server disconnected` is recorded twice above as a thing a *read* must survive and a *write* must not be retried for, and both times the remedy was `read_with_retry` applied by hand. Applied by hand it reached **3 of 679** `.execute()` sites, so the pages nobody had retried were exactly the pages that failed. Measured on the running server while proving the deploy's performance gate: `/teacher/results` answered **500 twice in 28 seconds** from an un-retried `exams` select in `app/routes/teacher.py`, with the data fine. The worse consequence is on the deploy — the release-to-release gate counts **5xx against a baseline that had none**, so a fault that predates a release rolls that release back: reproduced here, **2 of 3 runs of unchanged code were refused**, and the flakiness stopped only when the 5xx did. So the retry moved onto the client. `app/utils/supabase_retry.py` wraps the supabase client once, `create_app` installs it, and every read through `get_supabase()` retries by default while the hand-written sites keep working unchanged. Two rules hold in it: **whether a query may be retried is decided at `execute()` time** from the builder's own `http_method` (GET/HEAD retried; POST/PATCH/DELETE not, because a dropped connection does not say whether the server ran the statement and re-sending an insert can apply it twice), and a genuine `postgrest.APIError` is raised on the first attempt, so this cannot become a blanket "try again" that hides a bad column. Measured after: **0 errors in 222 requests across four runs**, where the same load produced 5xx before. The first version of the wrapper did **nothing** — it demanded `http_method` on the object `table()` returns, and that object is a bare `SyncRequestBuilder` that has none, so the first step went unwrapped and every later step with it, while all four fake-based tests passed; the suite now drives postgrest's **real** builders (against a placeholder project, nothing dialled) and a mutation harness keeps them honest. Guards: `tests/unit/test_query_retry.py` (a GET is retried, a HEAD count is, a chained `table().select().eq().execute()` is, a write of every method is not, an API error is raised at once, non-builders pass through, `table()`/`rpc()` are wrapped on the real builder shape, and `create_app` installs the wrapper) — mutation-checked, **9/9 injected defects caught** (`.freebuff/mutate_query_retry.py`). Still open and unrelated to the data path: under contention on this laptop a fourth run was refused for a *latency* divergence (p50 2.26x), which is the gate's two-strike rule behaving as designed on a busy machine rather than a fault in a release.

- **A teacher can hand a report to somebody with no account, and the three properties that make that safe are the feature.** `/teacher/analysis/<exam>` now carries a Share control: one button mints `/r/<token>`, the URL is shown with its open count and its expiry, and Stop sharing revokes it. **The token is the permission** — `secrets.token_urlsafe(32)`, 256 bits, stored as-is on purpose (a hash would make the one moment a teacher can read their own link the moment it is created, and a link nobody can find again is a link they paste somewhere they cannot take back; the controls that actually bind are the 256 bits, `031_analysis_share_links.sql`'s RLS with no policy for anybody but the service key, and a revoke that takes effect on the next request). Unknown, expired and revoked are **one 404**, because telling a stranger which one it was tells them a token existed, and the last two are read from the *presence* of a date rather than from its parseability: each column is only ever written to stop a link, so a `revoked_at` this code cannot parse still revokes — the permissive reading of a corrupt date is a link that outlives the report it points at. **What a stranger may see is decided in code, not in markup**: `_chart_payload(..., public=True)` empties `people` (the payload is what reaches the browser, so "nothing draws it yet" stops being true the moment somebody adds a table), `analysis_report`'s three documents drop their people section and blank the `Key` column (the answer key in spreadsheet form), and the page's key marker is one condition — `option.key and not public` — rather than four places inside one chip. The CSV's teacher copy and its shared copy are compared **section by section**, because "the students section is gone" and "everything else survived" are two claims and the defect worth guarding is a redaction that takes a table of statistics with it. Two defects came out of proving it rather than testing it: `create()` reported failure after a write whose *response* was dropped (`RemoteProtocolError: Server disconnected`, the row already committed), so a teacher was told "try again" while a live link sat in the table — retrying blind would mint a second link, so the recovery is a **read**, which is the one safe question to ask of a non-idempotent write; and the live probe first read 404 on every link because the dev server predates a route it had not been restarted into — Jinja and Python both cache for the life of the process, the lesson already recorded for templates. Guards: `tests/unit/test_analysis_share.py` (66 tests — the token, the three refusals, the unparseable dates, an unmigrated table, minting, revocation across several rows, counting, the card's URL, four route-level refusals through the real app, the payload and all three documents, the rendered page in both states, and the migration's columns and policy), mutation-checked **19/19 caught** (`.freebuff/mutate_share_link.py`). Verified live against the production project (`.freebuff/prove_share_link.py`, `23/23`): migration 031 trialled in a rolled-back transaction with `--require-idempotent` and then applied with a recovery point, and on the real exam `38404a0e` — a link read anonymously with 5 item rows, 10 option chips, no key marker, no share control, no student name, no `answer_key` anywhere in the payload, the open counted `2 -> 3`, an unknown token 404, the teacher's own page still a 302 to login, and the link dead on the very next request after Stop sharing.
